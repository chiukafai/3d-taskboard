# WorkBuddy 3D 办公室任务看板 · 项目说明

> 用途：一份讲清「这个看板项目由哪些文件组成、谁是核心、谁是可清理冗余」的总说明。
> 适用对象：Kingsley（财务总监 / 市场营销，非技术背景）。技术名词均附括号中文说明。
> 最后核对：2026-08-10

---

## 一、项目一句话简介

这是一个用 **3D 办公室** 可视化呈现任务的看板：把 7 大职能（CEO / CFO / CTO / CPO / CMO / COO / CRO + 会议室 meeting）的任务，按部门摆进对应的虚拟办公室里，方便一眼看清「谁在干什么、和谁协作」。

任务数据由 **两条管线** 汇入（详见第四节），看板主文件双击即可打开。

**运行方式**：双击 `office-3d-taskboard.html`（首次打开需联网，会从 CDN 加载 three.js 三维引擎与 gsap 动画库）。

---

## 二、文件主次关系（层级图）

```
★ 第一层 · 核心（看板能跑起来必须存在）
└─ office-3d-taskboard.html          看板主文件（双击打开）
   ├─ 加载 tasks-data/tasks.js        主任务数据
   └─ 加载 tasks-data/inbox_tasks.js  导入任务数据（合并显示）

✦ 第二层 · 数据管线（生成上面的数据）
├─ tasks-data/extract-tasks.py        自动管线：读任务源 → 生成 tasks.js
│   └─ 依赖 tasks-data/keymap.json     部门映射规则（关键词→职能）
└─ tasks-data/import_inbox.py         手动管线：解析 inbox/*.md → 生成 inbox_tasks.js

◆ 第三层 · 输入源 / 入口
├─ inbox/任务清单_*.md                待导入的任务清单（board-export 技能产出）
│   ├─ inbox/_imported/               已导入、归档的 .md
│   └─ inbox/processed/               处理过的 .md
└─ ~/.workbuddy/task_log.md 等        自动管线的数据源（WorkBuddy 标准路径）

■ 第四层 · 配置 / 技能（驱动数据汇入）
├─ board_export_skill.md              board-export 技能「唯一真源」(SSOT)
└─ .workbuddy/skills/board-export/    已安装的技能副本（与 SSOT 同内容）

▣ 第五层 · 参考文档（非运行必需，建议保留）
├─ board_capture_guide.md            看板截图指南
├─ WB看板配置-001.md                  看板配置记录
└─ tasks-data/README.md              数据管道迁移指南（含一处过时路径）

✖ 第六层 · 可清理（非必需 / 冗余）
├─ office-3d-taskboard.html.bak(.bak3~.bak6)  看板备份快照（回滚用）
├─ board_export_skill-副本20260810230030.md   旧版技能副本
├─ import_inbox.py（根目录）           旧版重复脚本（用 tasks-data/ 版）
├─ __pycache__/                       Python 缓存（自动再生）
└─ .DS_Store（根目录 + inbox/）         macOS 元数据
```

---

## 三、文件清单与定位（总表）

| 文件 | 层级 | 作用 | 必需？ | 备注 |
|---|---|---|---|---|
| `office-3d-taskboard.html` | ① 核心 | 3D 看板主文件，双击打开即渲染办公室与任务 | ✅ 必需 | 真正的交付物 |
| `tasks-data/tasks.js` | ① 核心 | 主任务数据（`window.TASK_DATA`，约 55+ 条） | ✅ 必需 | 由 `extract-tasks.py` 生成 |
| `tasks-data/inbox_tasks.js` | ① 核心 | 导入的任务数据（`window.TASK_INBOX`） | ✅ 必需 | 由 `import_inbox.py` 生成 |
| `tasks-data/extract-tasks.py` | ② 管线 | 自动管线生成器：读任务源 → 写 `tasks.js` | ✅ 必需 | **勿删**，自动管线底座 |
| `tasks-data/keymap.json` | ② 管线 | 关键词→部门映射规则，供 `extract-tasks.py` 分类 | ✅ 必需 | **勿删**，缺则分类失败 |
| `tasks-data/import_inbox.py` | ② 管线 | 手动管线：解析 `inbox/*.md` → 写 `inbox_tasks.js` | ✅ 必需 | 技能引用此版（较新） |
| `inbox/任务清单_*.md` | ③ 输入 | 待导入任务清单（board-export 产出） | ⚠️ 按需 | 导入后移入 `_imported/` |
| `inbox/_imported/` `inbox/processed/` | ③ 输入 | 已处理 .md 归档目录 | ⚠️ 按需 | 空目录也保留 |
| `board_export_skill.md` | ④ 配置 | board-export 技能唯一真源 (SSOT) | ✅ 必需 | 改技能改这里 |
| `.workbuddy/skills/board-export/` | ④ 配置 | 已安装技能副本（与 SSOT 同内容） | ✅ 必需 | 触发 `/board-export` 等 |
| `board_capture_guide.md` | ⑤ 文档 | 看板截图指南 | ⚠️ 可选 | 参考用 |
| `WB看板配置-001.md` | ⑤ 文档 | 看板配置记录 | ⚠️ 可选 | 参考用 |
| `tasks-data/README.md` | ⑤ 文档 | 数据管道迁移指南 | ⚠️ 可选 | 含过时路径，无害 |
| `office-3d-taskboard.html.bak(.bak3~.bak6)` | ⑥ 清理 | 看板备份快照（5 份，4 个不同版本） | ❌ 非必需 | 回滚安全网，可留 |
| `board_export_skill-副本20260810230030.md` | ⑥ 清理 | 旧版技能副本（已过时） | ❌ 非必需 | 建议删除 |
| `import_inbox.py`（根目录） | ⑥ 清理 | 旧版重复脚本（与 tasks-data 版内容不同） | ❌ 非必需 | 建议删除，统一用 tasks-data 版 |
| `__pycache__/` | ⑥ 清理 | Python 字节码缓存 | ❌ 非必需 | 运行脚本自动再生 |
| `.DS_Store` | ⑥ 清理 | macOS 元数据 | ❌ 非必需 | 根目录 + inbox 各一份 |

---

## 四、双数据管线（核心机制）

看板任务来自两条**互不覆盖**的管线：

### 管线 A — 自动（task-logger 驱动）
```
~/.workbuddy/task_log.md
~/.workbuddy/memory/CONTEXT_INDEX.md
~/.workbuddy/MEMORY.md
workspace 每日日志
      │  （数据源）
      ▼
tasks-data/extract-tasks.py  +  tasks-data/keymap.json（规则）
      │
      ▼
tasks-data/tasks.js   ──►  HTML 的 loadTasks() 加载
```
- 适合：日常任务由 WorkBuddy 自动记录后，定期重跑脚本刷新看板。

### 管线 B — 手动 / 跨机（board-export 驱动）
```
会话中产出 任务清单_*.md
      │  （放入 inbox/）
      ▼
tasks-data/import_inbox.py
      │
      ▼
tasks-data/inbox_tasks.js  ──►  HTML 合并进 TASK_INBOX 显示
（源 .md 归档到 inbox/_imported/）
```
- 也支持看板内 📥 按钮：选 `inbox/` 文件夹 → 浏览器解析 → 下载 `inbox_tasks.js` → 放入 `tasks-data/` 刷新。

---

## 五、日常操作速查

| 场景 | 操作 |
|---|---|
| 刷新主任务（自动管线） | 终端运行 `python3 tasks-data/extract-tasks.py` |
| 导入一次会话任务 | 把 `.md` 放入 `inbox/` → 运行 `python3 tasks-data/import_inbox.py`（或看板📥按钮） |
| 修改技能逻辑 | 改 `board_export_skill.md`（SSOT），已安装副本自动同步 |
| 看板改坏想回退 | 用某个 `office-3d-taskboard.html.bak*` 覆盖主文件 |
| 清理冗余 | 删第六层标注的文件（见第三节） |

---

## 六、注意事项（踩坑预警）

1. ⚠️ **`tasks.js` 是 WPS 云占位文件**：本机若未「始终保留在此设备」，浏览器读不到会回退到示例任务。路径：右键 `tasks-data/tasks.js` → 始终保留在此设备。
2. ⚠️ **勿删 `extract-tasks.py` 与 `keymap.json`**：它们是自动管线的底座，删了 `tasks.js` 无法自动更新（旧日期 ≠ 废弃，今天被运行过）。
3. ⚠️ **两套 `import_inbox.py` 并存**：根目录一份（旧、较大）、`tasks-data/` 一份（新、技能引用）。统一用 `tasks-data/` 版，避免改错文件。
4. ⚠️ **首屏需联网**：three.js / gsap 走 CDN，离线时 3D 场景空白。
5. ⚠️ **备份文件 `.bak` 不自动清理**：目前 5 份里 `.bak`=`.bak4`、`.bak5`=`.bak6` 是逐字节重复，可删重复份但建议至少留 3 个不同历史版。

---

## 七、文件关系速记（给非技术同学）

- **看板 = 房子**（`office-3d-taskboard.html`）
- **任务 = 家具**（`tasks.js` + `inbox_tasks.js`）
- **家具怎么来**：一条自动生产线（extract 脚本+规则）每天造，一条手工线（import 脚本）把临时清单搬进来
- **清单草稿箱** = `inbox/`
- **说明书** = 本文件 + 第五节参考文档
- **废料/旧货** = 第六层那些 `.bak`、副本、缓存，可定期扔

---

## 八、3D 家具系统与性能优化（2026-09-11 深度检查）

> 本节覆盖第 3-4 节的旧描述中已过时的部分（家具系统与首屏机制已重构）。

### 8.1 文件清单补充（原表未收录）

| 文件 | 作用 | 必需？ |
|---|---|---|
| `all-models.js` | **高清家具模型包**（9 类 GLB 以 base64 内嵌，3.4MB）。暴露 `window.OFFICE_MODELS` | ✅ 必需（缺失则回退程序化家具） |
| `_verify/` | 验证脚本区：性能测量、门交互回归、布局审计、清理脚本 | ⚠️ 可选（开发用） |
| `_shots/` | 渲染截图证据（各阶段效果图） | ⚠️ 可选 |
| `office-3d-items/` | 早期单件模型浏览器（独立小工具，非看板依赖） | ❌ 非必需 |
| `office-sample-cfo.html` | CFO 房早期样板（已被主文件取代） | ❌ 非必需 |
| `cfo-models.js` | **旧模型包（3.2MB），已无任何代码引用** | ❌ 可删 |

### 8.2 家具系统怎么工作（一句话版）

看板每间房有一套**高清 GLB 家具**（`all-models.js`）和一套**程序化兜底家具**（代码现画）。
现在只在**模型包缺失**时才画兜底家具 —— 这是 9/11 修掉的最大性能与观感问题（此前两套叠加，出现"双重家具"）。

### 8.3 八间房布局表（单领导办公室 · **2026-09-11 v5 错落非对称 + 宽走廊**）

> ⚠️ 本节已按 **v5**（v4 平面图 + 走廊加宽 + 过道清障）重写。v3 的"四排方正"布局已废弃 ——
> 布局的核心诉求一直是**故意不对称**：西侧退台（E 向回退）、房间面宽 5.8–8.2m 不等、进深 4.6–8.2m 不等、
> 偏心圆厅（接待厅不在中轴上）、西北入口前庭 + 东南侧庭。
> v5 在此之上**只动室内交通**：主廊 3.0→4.2m、西支廊 2.8→4.0m / 1.6→2.8m，并清空走廊内全部陈设。

**包络 29.6m（x 东西）× 21.6m（z 南北）**，坐标原点在西北角，**z 越大越靠南**。

```
z=0.0  ┌前庭┐                    ┌──── CMO 营销中心 ────┐
z=1.6  │(室 │                    │  8.2×8.2 = 67.2㎡     ├── CTO 技术中心 ──┐
z=3.6  │外) ├──── CRO 风控 ──────┤                      │  5.8×6.6 = 38.3㎡│
z=5.6  ├────┴─┐  6.2×4.6=28.5㎡  │                      │                  │
       │ CPO  │                  │                      │                  │
z=8.2  │ 产品 │──门────────────────门──────────────────────门───────────────┤
       │ 设计 │     东西主廊 · 净宽 4.2m（v5 加宽）                          │
z=11.0 │ 6.6 ─┤   ┌─○ 偏心圆厅 接待厅 (9.4,10.8) r=1.5（已清空）             │
       │ ×5.4  │   │                                                        │
z=12.4 ├───────┤   │  ┌──── COO 运营中心 ────┬── CEO 战略办公室 ──┐         │
       │ MT 战略│   └──┤  8.0×7.0 = 56.0㎡    │  6.6×6.6 = 43.6㎡ │  侧庭   │
z=16.4 │ 会议室├──────┤  西支廊上段 4.0m     │                   │  (室外) │
       │ 6.4×5.4│ CFO  │                      │                   │         │
z=19.4 │ =34.6 ┤ 财务 ├──────────────────────┴───────────────────┘         │
z=21.6 └───────┴──────┘   西支廊下段 2.8m（原 1.6m，并入 7.7㎡ 死区）        │
```

| 房间 | 平面位置 (x1..x2, z1..z2) | 尺寸 | 面积 | 门位置 | 模型件数 |
|---|---|---|---|---|---|
| CPO 产品设计室 | 0.0–6.6, 5.6–11.0 | 6.6×5.4 | 35.6㎡ | 东墙 x=6.6, z=9.6（朝主廊，v5 随墙线居中） | 8 |
| MT 战略会议室 | 1.0–7.4, 11.0–16.4 | 6.4×5.4 | 34.6㎡ | 东墙 x=7.4, z=13.6（朝西支廊） | 7 |
| CFO 财务中心 | 2.2–8.6, 16.4–21.6 | 6.4×5.2 | 33.3㎡ | 东墙 x=8.6, z=19.0（朝西支廊下段） | 10 |
| CRO 风控中心 | 9.4–15.6, 3.6–8.2 | 6.2×4.6 | 28.5㎡ | 南墙 z=8.2, x=12.6（朝主廊） | 8 |
| CMO 营销中心 | 15.6–23.8, 0.0–8.2 | 8.2×8.2 | 67.2㎡ | 南墙 z=8.2, x=17.6（朝主廊） | 17 |
| CTO 技术中心 | 23.8–29.6, 1.6–8.2 | 5.8×6.6 | 38.3㎡ | 南墙 z=8.2, x=26.4（朝主廊） | 10 |
| COO 运营中心 | 11.4–19.4, 12.4–19.4 | 8.0×7.0 | 56.0㎡ | 北墙 z=12.4, x=15.2（朝主廊） | 12 |
| CEO 战略办公室 | 19.4–26.0, 12.4–19.0 | 6.6×6.6 | 43.6㎡ | 北墙 z=12.4, x=22.6（朝主廊） | 10 |

**公共区（走廊 + 支廊 + 圆厅）合计 132.5㎡**，房间合计 337.1㎡，室内合计 469.6㎡，公共占比 **28.2%**（v4 为 24.5%）。
包络 29.6×21.6 与覆盖率 73.4% **与 v4 完全一致**（只动室内隔墙，外轮廓未动）。

| 公共构件 | 说明 |
|---|---|
| 东西主廊 | z 8.2–12.4（**南北净宽 4.2m**，v4 为 3.0m），x 7.4→29.4，贯通全楼东西 |
| 西支廊 | x 7.4–11.4：上段（z 12.4–16.4）**净宽 4.0m**；下段（z 16.4–21.6）**净宽 2.8m**；串起 MT / CFO |
| 中央接待厅 | 圆形 **Ø3.0m**（v5 由 Ø3.8 收小），圆心 **(9.4, 10.8) r=1.5** —— 故意不在几何中轴 (14.8) 上；**内部清空，仅留圆形石盘 + 外环带作视觉锚点** |
| 西北入口前庭 | 室外，x 0–6.6 / z 0–5.6（绿地 + 铺装 + 景观陈设，**保留**） |
| 东南侧庭 | 室外，x 26.2–29.6 / z 12.6–19.0（**保留**） |
| 主入口 | 北侧 **x=8.0, z=5.6**，双开门（外开）+ 雨棚立柱 |
| 疏散口 | 东侧 **x=29.4, z=10.5**，单门（外开） |

> ✅ **8 樘房门 + 2 樘公共门共 10 樘，全部朝公共区、且全部外开**（2026-09-11 修复：南/北墙门此前误做内开，已由 `openSign` 纠正）。
> ✅ **走廊/圆厅内实体家具 = 0 件**（v5 拆除 9 组 15 件）；室外前庭/侧庭景观按约定保留，不计入"过道"。
> ✅ 所有房间**面宽、进深全不相等**，且**无任何一条透视轴贯通到底**（"为什么不对称"见平面图说明栏）。

### 8.4 性能优化记录（同日同机对比）

| 指标 | 优化前 | 优化后 | 变化 |
|---|---|---|---|
| 首屏内容绘制 (FCP) | — | **564 ms** | 页面外壳秒出 |
| DOMContentLoaded | 6066 ms | **3959 ms** | −35% |
| 8 房模型全部就位 | 18484 ms | **9295 ms** | −50% |
| 场景网格数 (meshes) | 590 | **453** | −23% |
| 材质数 | 278 | **190** | −32% |
| 模型包体积 | 3.99 MB | **3.4 MB** | −15% |
| 主文件 | 171 KB | **158 KB** | −8% |

**做了什么**（6 项）：
1. **双重家具修复**：模型包可用时跳过程序化兜底家具构建（最大收益）
2. **删除死代码**：10 个从未被调用的函数（贝壳篮/宝箱/救生圈/冲浪板…）+ 废弃示例任务表 + 未用变量 = 约 11 KB
3. **材质缓存**：同色材质全局复用（`_matCache`），材质数 −32%
4. **脚本延迟加载**：`defer` 让 HTML 先绘制，首屏从数秒空白 → 0.56 秒
5. **CDN 模块预加载**：`modulepreload` 并行取回 three.js 等 6 个模块，无重复下载
6. **阴影按需更新**：场景静止时不重算 2048² 阴影贴图，仅门开合/模型加载时刷新

### 8.6 布局重排与座向修正（历史留档 · v3 时代 · 已被 8.9 的 v4 取代）

> 🗄️ **本节描述的是 v3 布局（24×18、中央大厅、四排方正）。v4 已整体重做，见 8.9。**
> 保留此节是因为其中"**模型自身朝向**"的实测结论（`exec_chair`/`sofa` 面朝 +z，`office_chair` 面朝 −z）
> 在 v4 中**依然成立且继续使用**，是排椅子时的硬约束，不要删。

用户截图标记了 3 处"不合理"，排查后发现**根因是结构性的**（不止表面 3 处）：

| # | 用户标记 | 真实根因 |
|---|---|---|
| ① | 一块大板挡在门口 | CMO 白板挂在旧大厅东端 (17.65, 9)，正堵 CPO 门；且背面（黑板面）朝外 |
| ② | CFO 门牌位置不对 | 门牌算法**永远贴门墙中心**，而 CFO 门牌算出的 (20, 12.75) 落在 CPO 房内 |
| ③ | CFO 房门设计不合理 | CFO 北墙门(z=13) 与 CEO 南墙门(z=5) 都被 CPO 的房体(x18~24, z5~13) **整体封死** |

**处置**：不是"挪门牌"，而是重排布局让所有门通向外向空间。

- **CPO 迁到东北角**（原 CEO 位置）：CPO 房体不再压住任何人的门
- **CEO 迁到南排正中**（原空位）：南排变成 CTO / CEO / CFO 三间，CEO 居正中
- **CMO 大厅打通**：由 18×8 扩为 **24×8**，横贯全楼，成为唯一交通空间
- 门牌改为**放在门墙较长实墙段**（避开门洞），并支持自定义宽度 `pw`；背板文字做镜像
- 侧墙门（左/右）门扇位置修正（合页侧算错，偏移 0.8m）
- 白板挪到大厅东墙（`x=cx+hw-0.35, rotY=-π/2`），面朝大厅
- 中庭装饰重组：删东走廊组、南露台组移入大厅西端作室内花园、入口绿植让开门道

**第二轮追加修正 —— 座向**（重排后新发现，静态审计漏检）：

实测 GLB 模型自身朝向（顶点分布分析）发现**两个椅模型朝向相反**：

| 模型 | 靠背位置 | r=0 时面朝 |
|---|---|---|
| `exec_chair` | −z | **+z** |
| `sofa` | −z | **+z** |
| `office_chair` | +z | **−z** ← 与上两者相反 |

据此修正 3 处：**COO**（办公椅落在桌子外侧、且物理嵌进后墙记忆屏）、**CPO**（重排时把桌/椅前后颠倒）、**CEO**（访客椅背对办公桌）。统一为「椅贴桌的靠门一侧、人面朝桌（背对门）」，与 CFO/CRO 样板一致。

> ⚠️ 遗留：CFO 房 2 张访客椅（`office_chair` r=π）背对办公桌 —— 因用户要求"CFO 房配置保持不变"未改，已记录在审计脚本的 EXEMPT 白名单中。

### 8.7 布局审计脚本（`_verify/audit_v5.py`）

不依赖截图，纯静态几何检查，**可复现、可回归**（截图法在无 GPU 环境不可靠）：

```
python _verify/audit_v5.py          # v5 布局（当前主用，基准 plan_v5.json）
python _verify/audit_v4.py          # v4 布局（历史留档，基准 plan_v4.json）
python _verify/audit_v2.py          # v3 布局（历史留档）
```

| 检查项 | 内容 |
|---|---|
| ① | 房间矩形互不重叠、公共区不压房间（0.1m 栅格） |
| ② | 每樘门朝外 0.35m 探针格归属为「公共区」（不被邻居房体封死） |
| ③ | 门牌不压门洞、不越出房间 |
| ④ | 家具包围盒（旋转感知）在房内、不压门洞迎面区 |
| ⑤ | 座位朝向：主椅是否面朝办公桌（点积 > 0.6） |
| ⑥ | 家具是否穿插墙上记忆屏/白板（含局部↔世界坐标转换） |

当前状态：**六项全部通过（0 问题）**。已验证该脚本能检出修正前的 6 处朝向问题、COO 椅嵌屏问题、COO 文件柜越墙问题。

**净空实测脚本（v5 新增，判"走廊够不够宽"用它，别靠目测）**

```
node _verify/probe_clearance_v5.mjs   # 对公共区逐格从 y=1.6m 垂直下打射线
```

输出三件事：① 主廊沿 x 逐 0.2m 的**逐断面净宽**（v5 实测 110 个断面全部 4.2m）② 西支廊逐断面净宽
③ 公共区步行高度带（0.20~1.60m）内的**残留障碍物聚类**（v5 = 0 件）。

### 8.8 已知限制

1. ⚠️ **首屏仍需联网**：three.js / Draco 解码器走 CDN（esm.sh / jsdelivr）。**不能改成全本地**——因为 `file://` 双击打开时浏览器禁止加载本地 ES 模块，本地化会让双击打不开。
2. ⚠️ **模型库缺口**：无「会议长桌 / 服务器机柜 / 壁挂白板 / 监控大屏」模型 → 会议室用沙发茶几、CTO 用文件柜替代。
3. ⚠️ **Draco 解码**：GLB 为 Draco 压缩，依赖 CDN 的 wasm 解码器。
4. ⚠️ **本机硬件差异**：上述耗时在无 GPU 的测试环境测得，你本机（有显卡）会明显更快。

### 8.9 v4 错落非对称布局重做（2026-09-11 · 依据用户 `plan_v4_light.png`）

**触发**：用户给出 v4 平面图（故意错落、非对称、偏心圆厅），要求"根据这个布局重新设计"。
这不是挪几件家具，而是**换掉整栋楼的骨架** —— 包络从 24×18 放大到 **29.6×21.6**（+48% 面积），
8 个房间的位置、尺寸、门的朝向、家具摆位、墙体、地板、相机、灯光阴影范围**全部重算**。

**做法：几何先行，代码后写**（避免"边改边猜"）

| 步骤 | 脚本 | 产出 |
|---|---|---|
| ① 平面几何校验 | `_verify/gen_plan_v4_geom.py` | `plan_v4.json`（0.1m 栅格：0 重叠 / 8 门全通公共区 / 镜像自对称仅 COO 1 处） |
| ② 平面转 3D 坐标 | `_verify/gen3d_v4_data.py` | `gen3d_v4.json`（ZONES / PUB_FLOORS / PUB_WALLS / PUB_DOORS / PAD_RECTS） |
| ③ A 阶段落码 | `_verify/relayout_v4_a.py` | 替换 ZONES、新增 `SITE/LOBBY/COURT/YARD/ENTRY/EXITX` 常量、新增 `createPublicWalls()`、重写 `createMainFloor()` |
| ④ B 阶段落码 | `_verify/relayout_v4_b.py` | 重写 8 组 `*_SHOW`、CFO 装饰、CMO 程序化兜底、记忆屏位置、`createAtrium()`、相机/雾/灯光/阴影 |
| ⑤ 静态审计 | `_verify/audit_v4.py` | 6 项检查全绿 |
| ⑥ 浏览器验证 | `_verify/verify_v4.mjs` | 8/8 模型 / 8 门全外开 / 门洞净空 / 0 JS 报错 |
| ⑦ 足印复核 | `_verify/footprint_probe.mjs` | 149×109 @0.2m 垂直射线足印 → 房体/公共地板**无空洞** |

**v4 落码时踩到并修掉的 5 个坑**

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| ① | CFO 文件柜"插进"北墙 | 柜体旋转后半径 1.44m，放在 (1.30,−2.20) 距北墙显示屏仅 1.0m | 移到 (2.55,−2.20)，程序化兜底同步；原址补绿植 |
| ② | 审计报"公共区压房间 1640 格" | `fill()` 把"公共压公共"也计成冲突 | 只在压到**非公共**归属时计数 |
| ③ | 审计误报"家具压门洞" | 门包围盒 x 边界取错索引 (`doorbox[1],doorbox[2]`)，z 边界同理 | 改为 x=(`doorbox[2]`,`doorbox[0]`)、z=(`doorbox[3]`,`doorbox[1]`) |
| ④ | 会议室访客椅 / CEO 办公椅贴门墙太近 | 房体变大后椅子仍沿用旧偏移 | 分别移到 (1.55,±1.10) 与 (−0.90,−1.75) |
| ⑤ | **南/北墙的门向内开**（cro/cmo/cto/coo/ceo 5 樘） | `animateDoors` 的旋向 sign 是全局常数，但东/西墙与南/北墙的"朝外"局部旋向相反 | 门枢新增 `userData.openSign`（front/back = −1，right/left = +1），`sign = (双开±1) × openSign` |

> 📌 **教训（写给下次改布局的自己）**：v4 这类"整栋重做"必须**先跑几何校验再落码**。
> 直接从 HTML 里手改 8 个 ZONES 会连带出错——门朝向、家具偏移、记忆屏位置、相机范围是**四处联动**的，
> 靠肉眼盯代码必然漏。本次 `gen_plan_v4_geom.py` 先用 0.1m 栅格把"重叠 / 门是否通公共区 / 是否镜像自对称"
> 全部锁死，落码阶段才只剩"坐标搬运"这一件机械工作。
>
> 📌 **另记**：截图法在无 GPU 测试环境下**不可靠**（画布 alpha 合成 + 页面背景色会伪造出"缺失区域"的假象）。
> 判定"布局对不对"应优先用**射线足印**（`footprint_probe.mjs`）或**静态几何**（`audit_v4.py`），截图只作观感参考。

**备份与回退**：改动前已存 `office-3d-taskboard.html.bak_pre_v4_20260911`（163,609 B，v3 布局）。
如需回退：`cp office-3d-taskboard.html.bak_pre_v4_20260911 office-3d-taskboard.html`。

### 8.10 v5 走廊加宽 + 过道清障（2026-09-11 · 依据用户截图标注）

**触发**：用户看完 v4 落地的 3D 截图后反馈 ——"整体大致可以了，但现在清除过道上的'杂物'（如截图所示），
并且适当增大过道宽度面积，请将你理解的用平面图示先给我确认"。先出**平面图方案**（`平面图方案-走廊加宽版.html`）
请用户确认，确认后（"可以，继续"）才落地到 3D 看板。

**总原则：只动室内隔墙，外轮廓 / 退台 / 房间外墙一律不动。**
好处是相机、雾、光照、地面、建筑垫层都不用重算 —— 包络仍是 29.6×21.6，覆盖率仍是 73.4%。

**① 走廊加宽（3 个构件 + 1 樘门联动）**

| 构件 | v4 | v5 | 怎么做到 |
|---|---|---|---|
| 东西主廊（南北净宽） | 3.0 m | **4.2 m** | 北排 CRO/CMO/CTO 南界 8.6→8.2；南排 COO 北界 11.6→12.4（CEO 12.4 不动） |
| 西支廊上段（东西净宽） | 2.8 m | **4.0 m** | 东界 10.2→11.4 |
| 西支廊下段 | 1.6 m | **2.8 m** | 同上；顺带把 x10.2~11.4 / z13.0~19.4 这块 **7.7㎡ 建筑内死区**并进走廊 |
| 接待圆厅 | Ø3.8 m | **Ø3.0 m** | 圆心 (9.4,10.8) 偏心位置不动 |
| CPO 门 | 东墙 z=8.0 | 东墙 z=**9.6** | 居中于加宽后的主廊；其余 7 樘门位置不变 |

**面积账**：房间 351.6 → **337.1㎡**（让出 14.5㎡）· 公共 114.0 → **132.5㎡**（+16.2%）
· 室内 465.6 → 469.6㎡ · 公共占比 24.5% → **28.2%** · 包络与覆盖率不变。

**② 过道清障（9 组 15 件，全部来自 `createAtrium()`）**

先用 `_verify/probe_corridor_clutter.mjs` 扫一遍场景（遍历 scene 根子节点 → 世界包围盒 → 落在公共区且底 y<1.85），
拿到**实测清单**而不是照截图数：

| # | 坐标 (x, z) | 物件 |
|---|---|---|
| 1 | (9.40, 11.95) | 接待台（柜体 2.9×0.7 + 台面 3.0 + 踢脚）+ 台面屏 + 马克杯 + 笔筒 |
| 2 | (9.40, 12.70) | 访客椅 |
| 3 | (7.88, 10.55) | 等候长椅（西弧） |
| 4 | (10.92, 10.55) | 等候长椅（东弧） |
| 5 | (9.40, 10.65) | 中心花坛陶盆 + 绿植（高 1.35m，最挡视线的那个） |
| 6 | (8.45, 9.55) | 贝壳喷泉 |
| 7 | (10.35, 9.55) | 贝壳罐 |
| 8 | (12.60, 10.10) | 主廊缆绳卷（西） |
| 9 | (23.40, 10.10) | 主廊缆绳卷（东） |

清完后**走廊/圆厅内实体家具 = 0 件**，只留地面铺装 + 圆形石盘 + 外环带。
**西北入口前庭与东南侧庭的景观陈设（长椅/绿植/喷泉/缆绳卷）按约定保留** —— 那是室外场地，不属于"过道"。

**③ 落地与验证链路**

| 步骤 | 脚本 | 结果 |
|---|---|---|
| ① 平面几何校验 | `_verify/gen_plan_v5_geom.py` | `plan_v5.json`（0 重叠 / 8 门全通公共区 / 主廊逐点实测 4.2m） |
| ② 平面转 3D 数据 | `_verify/gen3d_v5_data.py` | `gen3d_v5.json`（ZONES / PUB_FLOORS 5 块 / PUB_WALLS 5 段 / PAD_RECTS 9 块） |
| ③ 一次性落码 | `_verify/relayout_v5.py` | 7 处改动（注释 / LOBBY.r / 三张表 / ZONES / createAtrium 清障 / 家具微调 / 生成 audit_v5） |
| ④ 静态审计 | `_verify/audit_v5.py` | **6 项全绿**（曾报 COO 2 个文件柜越北墙 → 已南移 0.30m） |
| ⑤ 浏览器验证 | `_verify/verify_v5.mjs` | 8/8 房间、8/8 模型、**10 樘门全外开**、396 mesh / 212 材质 / 281 万三角面 |
| ⑥ 净空实测 | `_verify/probe_clearance_v5.mjs` | 主廊 110 个断面**全部 4.2m**；西支廊上段 4.0m / 下段 2.8m；公共区障碍物**0 件** |
| ⑦ 视觉复核 | `_verify/shots_v5.mjs` + `shots_v5_corr.mjs` | 俯视 / 8 房 / 主廊人眼 / 圆厅俯视 等 |
| ⑧ 验收对照图 | `_verify/make_compare_v5f.py` | `_shots/v5_落地对照_plan_vs_3d.png`、`_shots/v5_走廊净宽与清障.png` |

**落码踩坑**

| # | 现象 | 根因 / 修法 |
|---|---|---|
| ① | `relayout_v5.py` 报"找不到 ZONES 行：ceo" | ZONES 最后一行结尾没有逗号，正则 `\},$` 不匹配 → 改成 `\},?$` |
| ② | 审计报 COO 两个文件柜"出房间" | COO 进深 7.8→7.0 后，柜体 z 越出北墙 0.168m → 南移 0.30m |
| ③ | `verify_v5.mjs` 多出一条 `THREE.WebGLProgram: Shader Error 0` | **非本次改动引入** —— 用 `cmp_v4_v5_errors.mjs` 对 v4 备份同条件复测，v4 也有同样报错，属无 GPU 环境（SwiftShader）通病 |
| ④ | 对照图里 `✅` 渲染成豆腐块 | msyh 缺 U+2705/U+2713/U+2714/U+2194 → 统一改用 `√`（U+221A）、`→`、`vs` |

**回退**：改动前已存 `office-3d-taskboard.html.bak_pre_v5_20260911`（172,493 B，v4 布局）。
如需回退：`cp office-3d-taskboard.html.bak_pre_v5_20260911 office-3d-taskboard.html`。

> 📌 **教训**：判"走廊够不够宽 / 有没有被家具挡住"必须用**射线实测**（`probe_clearance_v5.mjs`），
> 不要拿坐标相减估算，也不要目测截图 —— 墙体厚度、家具旋转半径、地面铺装都会影响真实通行净宽。

---

### 8.11 v5.1 三处修复（2026-09-11 · 依据用户截图反馈）

用户发 `Clipboard_Screenshot.png` 指出三个问题：**门牌字反 / CFO 地毯闪烁 / 办公桌千篇一律且每桌两台笔记本各带圆盘托底**。

**主文件 169,006 B → 173,147 B**；备份 `office-3d-taskboard.html.bak_pre_v51_20260911`（169,006 B = v5）。

#### 一、三处真根因

| # | 用户描述 | 真根因 |
|---|---|---|
| ① | 三个部门的字相反 | **东/西侧墙门**（`doorWall==='right'/'left'`）的牌面旋转角 `pr` 取 −π/2 / +π/2，使**牌面正面法线指向房内**；走廊一侧只能看到 `back` 背面 → 镜像字。涉及 **CPO 产品设计室 / 战略会议室 / CFO 财务中心**（正好三块）。附带 bug：`back` 背面又额外做了 `repeat.x=−1`，而整个牌组已绕 Y 转 π —— 等于**双重镜像** |
| ② | CFO 地毯不停闪烁 | 两块地毯都摆在 **y=0.095**（同高共面），x 向重叠 1.70m、z 向重叠 0.83m → **Z-fighting**。全场景扫描共发现 **14 处**同高重叠 |
| ③ | 桌面千篇一律 / 两台笔记本各一个圆盘 | 🔴 **对 `office_monitor` 模型的误判**：它其实是「显示器 + 键盘 + 鼠标 摆在 **Ø0.88m 圆木托底**上」的**一整套工位**（世界包围盒 ≈ 1.044 × 0.592 × 1.043m）。一个就占满整张 1.88×0.70 办公桌 → **每桌放两个 = 两套工位叠罗汉**，看起来很像是"两台笔记本各带一个圆盘托底"。且 8 间房的 `_SHOW` 阵列模板雷同 |

#### 二、改了什么（4 个幂等脚本 + 1 处顺带修）

| 脚本 | 内容 |
|---|---|
| `_verify/fix_v51_ab.py` | **A1** `makePlaque` 背面删掉 `repeat.x=−1` 镜像，正/背面共用同一张贴图；**A2** 东/西墙门 `pr` 取反（right → +π/2，left → −π/2），使正面朝走廊；**B** CFO 双毯按 z 切开 |
| `_verify/fix_v51_d.py` | 从 8 个 `*_SHOW` 阵列里**删掉全部 `office_monitor`（共 11 个）与 `laptop` 条目**；给每房 `exec_desk` 加不同木色 tint |
| `_verify/fix_v51_c.py` | 新增 `deskGrp()` / `createDeskMonitor()` 两个辅助函数 + `DTOP_HI=0.85` / `DTOP_PROG=0.90` 常量；在 `g.add(prog)` 前插入 **8 组桌面配置块**。**脚本内置重叠自检**（无冲突才允许 `--apply` 落盘） |
| 顺带修 | `createLaptop` 的屏幕原来 `rotation.y=π` + `z=−0.008` 贴到了盖子背面 → 改为 `rotation.y=0, z=+0.008` |

**统一约定**：所有桌面小物一律按**桌子局部坐标**摆放 —— 人坐 局部 **−Z**，桌长轴 = **X**，**所有屏幕朝 −Z**（正对使用者）。

**8 间房桌面配方（互不相同）**

| 房 | 桌位 / 旋转 | 配置 |
|---|---|---|
| CRO | (0.00, −1.50) r=π | 双显示器 + 键鼠 + 纸堆 + 笔筒 + 马克杯 |
| CTO | (0.60, −1.30) r=π | 显示器 + 笔记本 + 键鼠 + 笔筒 + 马克杯 |
| CEO | (−0.90, −1.15) r=0 | 显示器 + 笔记本 + 键鼠 + **座机** + 纸堆 + 马克杯 |
| CFO | (−1.90, 0.30) r=−π/2 | 显示器 + 笔记本 + 键鼠 + 笔筒 + 纸堆 + **座机** |
| COO | (0.30, −1.20) r=0 | 显示器 + 键鼠 + 笔筒 + 马克杯 + **托盘**（无笔记本） |
| CPO | (−1.70, 0.00) r=−π/2 | **笔记本 + 显示器**（顺序对调）+ 键鼠 + 纸堆 + 笔筒 + 马克杯 |
| CMO 桌1 | (−1.70, −1.90) r=π | 双显示器 + 键鼠 + 笔筒 + 马克杯 |
| CMO 桌2 | (1.30, −1.90) r=π | 显示器 + 笔记本 + 键鼠 + 纸堆 + 马克杯 |

#### 三、验收

| 关 | 工具 | 结果 |
|---|---|---|
| 静态审计 | `_verify/audit_v5.py` | **6/6 全绿**（座向 8/8 点积 +1.00；记忆屏 8/8 不相交） |
| 门牌朝向 | `_verify/verify_v51.mjs` | **8/8 正面朝外**，点积 0.82~0.967 |
| 地毯重叠 | `_verify/verify_v51.mjs` | 同 y 重叠 **`[]`** |
| 视觉复核 | `_verify/verify_v51.mjs` | 12 个视角全部出图（`_shots/v0_overview.png` … `v11_cpo_desk.png`） |
| 验收对照图 | `_verify/make_compare_v51.py` | `_shots/v51_修复对照.png`（左修前 / 右修后 + 三条修复说明） |

#### 四、⭐ 两条方法论（务必记住）

1. **在本沙箱里出图只能用 `_verify/grab.mjs`**。
   `page.screenshot()` / `canvas.toDataURL()` 会返回**全白**（std=0），根因是 renderer 是
   `new THREE.WebGLRenderer({antialias:true, alpha:true})`，**没开 `preserveDrawingBuffer`**，且无 GPU 时画布合成被丢弃。
   解法：渲染到离屏 `WebGLRenderTarget` → `readRenderTargetPixels` → 翻 Y → **手工用 zlib 编码 PNG**。
   导出 `encodePNG()` / `rtShot()` / `look()`（`look()` 会先把 OrbitControls 的 `minDistance` 9→0.05、极角放开，否则近景相机被控件覆盖）。
2. **判"某个 GLB 到底是什么"必须隔离渲染 + 实测包围盒，别信文件名**。
   `office_monitor` 这个名字骗过了第一轮判断，直接导致"两台笔记本各带圆盘托底"。
   做法见 `_verify/probe_monitor_face.mjs`：按世界包围盒把目标模型筛出、其余全 `visible=false`，从 +Z / −Z / 顶三角度出图 → 一眼看清结构。

#### 五、回退

```
cp office-3d-taskboard.html.bak_pre_v51_20260911 office-3d-taskboard.html
```
