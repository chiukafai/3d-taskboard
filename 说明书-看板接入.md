# 说明书 · 3D 看板任务接入（agent 无关版）

> 产物：`taskboard/` 引擎 + `board.config.json` + `Dockerfile` / `docker-compose.yml` + `agent-templates/`
> 建立日期：2026-09-17 · 版本 v1.0.0

---

## 1. 概述与用途

**要解决什么问题**：你手上有好几个 AI 助手（WorkBuddy、Claude Code、Hermes、各种脚本…），
每个都在干活、都有任务，但**干完了就散在各处，没有一个地方能一眼看全**。

**这套东西干三件事**：

| # | 能力 | 一句话 |
|---|---|---|
| 1 | **统一收件** | 任何 agent 做完事，用「发一个 HTTP 请求」或「往一个目录丢个文件」就能把任务录进来 |
| 2 | **随时查看** | 浏览器打开一个网址就是 3D 看板，任务按部门落在对应房间里；改动实时同步 |
| 3 | **一键部署** | 打包成 Docker 容器，能丢到 NAS/服务器上 24 小时常开，局域网任何设备都能看 |
| 4 | **看得清归属** | 部门是上报方判的还是引擎猜的，看板用不同标记区分（猜的打「部门待核」） |
| 5 | **建议认领**（可选） | agent 可以说"这条我能接"，但**人不批准就不开工** —— 保留人工审核关口 |

**关键设计：不绑定任何 agent、不写死任何路径。**
配置里出现的只有「相对路径 + 端口 + 令牌」，`~/.workbuddy` 这类目录**一个都没有**。
换 agent、换机器、搬到 NAS，都只改 `board.config.json`（或干脆用环境变量）。

---

## 2. 技术栈总览表

| 层 | 技术 | 用途 | 为什么这么选 |
|---|---|---|---|
| 前端看板 | 原生 JS（ES Module）+ three.js 0.157 | 3D 办公室渲染与交互 | 沿用现有看板，不重构 |
| 前端数据 | `fetch` + 轮询 | 从服务端拉任务、回写状态 | 不上框架，保持"双击就能开" |
| 后端 | **Python 3.12 标准库** `http.server` | 静态托管看板 + REST API | 零第三方依赖 → 镜像小、不用 pip、构建不联网 |
| 数据库 | **SQLite（WAL 模式）** | 任务库、去重、并发写入 | 单文件、免安装、多进程读写安全 |
| 投递箱 | 文件系统目录 | 离线 agent 录任务的通道 | agent 只要会写文件就行 |
| 部署 | Docker + docker-compose | 24×7 常开 | 一条命令起，数据挂载在宿主机不丢 |

> **为什么后端不用 Flask/FastAPI**：这套东西的核心诉求是「**在 NAS 上长期稳定跑、坏了简单重启**」。
> 依赖越少，能坏的地方越少。标准库已经够用（并发用 `ThreadingHTTPServer`）。

---

## 3. 编程语言

| 语言 | 版本 | 用在哪 |
|---|---|---|
| Python | 3.12+（本地实测 3.13） | 引擎全部：`taskboard/` 包、CLI、HTTP 服务 |
| JavaScript | ES2020（浏览器） | 看板的动态数据源、写回、UI 扩展 |
| Bash | 4+ | `agent-templates/*.sh` 接入示例 |
| PowerShell | 5.1+ | `agent-templates/windows-powershell.ps1` |
| YAML | compose spec | Docker 编排 |

---

## 4. 前端设计（看板侧）

**没有引入框架**，只在原看板里加了四件事：

| 加了什么 | 位置 | 效果 |
|---|---|---|
| 动态数据源 | `office-3d-taskboard.html` 的 `window.__applyTaskSources()` | 数据可重复刷新，不叠加不重复 |
| API 客户端 | `boardApiFetch / boardApiSync / boardApiWrite` | 拉任务、回写状态 |
| 数据源指示灯 | 左下角 `#api-badge` | 🟢 实时同步 · N 条 · 地址 ／ 🔴 服务未连接 |
| 来源标签 + 图例计数 | 任务卡片、右下角图例 | 卡片显示任务来自哪个 agent；图例显示「未完成/总数」+ 🔔待批数 |
| **认领待批浮层** | 左下角 `#claim-badge` + `#claim-tray` | 🔔 提示有 agent 申请接手，点开逐个批准/驳回 |
| **部门判定标记** | 任务卡片、详情面板 | 引擎猜出来的部门打灰色「部门待核」标；详情里写明是"上报方判定"还是"猜的"+理由 |
| **认领徽标 / 快捷按钮** | 任务卡片 | `🔔 N 人申请` + `✓`/`✕`；已认领显示 `✋ 执行方` + `↩ 撤销` |

**认领交互的两个刻意设计**：

1. **多个申请人时，卡片上的 ✓ 不会替用户选人**，而是展开浮层让人挑。用代码替人做"选谁"的决定，
   就把人工关口变成了走过场。
2. **静态数据（`file://` 或没连服务）也能看到徽标**（因为 `tasks.js` 里也导出了认领状态），
   但浮层里的按钮会禁用并提示"未连接看板服务" —— 不给人一个点了没反应的按钮。

**数据源优先级**（自动判断，无需配置）：

```
1) 地址栏 ?api=<地址>          → 用它（?api= 空串 = 强制关闭）
2) 服务端注入的 __BOARD_API_HINT → 服务端自己托管时自动注入（同源）
3) 都没有                       → 不联网，读同目录的 tasks-data/tasks.js
```

> ⚠️ **这里刻意不做"同源盲探测"**。早期版本是「只要页面用 http 打开就去试
> `/api/tasks.json`」，结果在普通静态服务器（`python -m http.server`）上每轮询一次
> 就制造一条 404 控制台噪声。现在改成**谁托管谁知道**：`taskboard/server.py`
> 在返回 HTML 时注入一行 `window.__BOARD_API_HINT=location.origin`，
> 静态打开的页面没有这个变量 → **一个请求都不发**。见 `server.py::_inject_hint`。

**刷新节奏**：默认每 20 秒拉一次（可用 `?poll=5` 改，`?poll=0` 关闭）。
你手动改状态或删任务时，会**立刻**回写服务端，不等轮询。
服务端连不上时**自动退回静态文件**，页面照常能用，只是数据不刷新；
连续失败 3 次会暂停轮询并在左下角显示可点击重试的指示灯（不再无限刷错误）。

**怎么在任意 agent 的浏览器里打开**：
```
http://<机器IP>:8787/                 ← 服务端托管，实时同步
http://<机器IP>:8787/office-3d-taskboard.html?api=http://另一台:8787&poll=10
```

---

## 5. 后端设计

### 5.1 API 一览

| 方法 | 路径 | 作用 | 要不要令牌 |
|---|---|---|---|
| GET | `/api/health` | 健康检查（容器 healthcheck 用） | 否 |
| GET | `/api/tasks` | 查任务，支持 `?dept= &status= &source= &agent= &claim=none\|pending\|approved &limit=` | 否 |
| GET | `/api/tasks.json` | 看板专用数据包（含统计 + 每条任务的认领状态） | 否 |
| GET | `/api/tasks.js` | 同上，直出 `window.TASK_DATA = …` | 否 |
| GET | `/api/summary` | 按部门/状态/来源/认领汇总 | 否 |
| GET | `/api/claims` | 查认领申请，支持 `?status=proposed &agent= &task= &limit=` | 否 |
| GET | `/api/queue` | **agent 的工作队列**：`?agent=<名字>` → 已批准的认领 | 否 |
| GET | `/api/events` | 最近操作流水 | 否 |
| GET | `/api/config` | 当前生效配置（令牌打码） | 否 |
| POST | `/api/tasks` | **录任务**（单条或数组）；带 `claim` 字段可顺手提申请 | 是（若设了 token） |
| POST | `/api/tasks/<id>/claim` | **提认领申请**（只登记意向，任务状态不变） | 是 |
| POST | `/api/claims/<id>/approve` | **批准**（人拍板）→ 任务转 `in_progress` + 落定 `assignee` | 是 |
| POST | `/api/claims/<id>/reject` | 驳回；若原是已批准状态则**撤销并退回 `todo`** | 是 |
| POST | `/api/claims/<id>/withdraw` | agent 主动撤回自己的申请 | 是 |
| PATCH | `/api/tasks/<id>` | 改字段（状态/优先级/说明…） | 是 |
| PUT | `/api/tasks/<id>` | 整条替换 | 是 |
| DELETE | `/api/tasks/<id>` | 删一条（连带删掉它的认领记录） | 是 |
| POST | `/api/ingest` | 立刻扫描投递箱 | 是 |
| GET | `/*` | 看板与模型包等静态文件 | 否 |

**令牌规则**：`board.config.json → server.token` 留空 = 不校验（**仅限受信内网**）。
设了值以后，所有写操作必须带 `Authorization: Bearer <token>`，读操作仍开放（看板要能读）。

### 5.2 数据库表结构

```sql
tasks               -- 任务主表
  id            稳定标识（dept + 去重键 的哈希，同一任务多次上报不会变）
  dept          部门 id（ceo/cfo/cto/cpo/cmo/coo/cro/meeting）或 _unassigned
  title/desc    标题 / 说明
  status        todo | in_progress | done | blocked
  priority      high | medium | low
  project       项目名
  source/agent  来源渠道（哪个 agent 录的）
  external_id   上游编号
  dedup_key     去重键（external_id 优先，否则用标题小写）
  due/tags/collaborators/auto/session
  dept_source   expert | guessed | none  ← 部门是上报方判的、引擎猜的、还是没认出来
  dept_reason   上报方给出的归类理由（可选，供人复核）
  assignee      认领被批准后落定的执行方
  created_at/updated_at/done_at

  UNIQUE(dept, dedup_key)   -- ← 「重复上报 = 更新」就靠这一条

claims              -- 认领申请（A+B 中间态的"建议认领"）
  id            cl-<哈希>（同一任务 + 同一 agent 共用，重复申请=更新）
  task_id       指向 tasks.id
  agent         谁在申请
  note          申请理由 / 打算怎么做
  status        proposed | approved | rejected | withdrawn
  created_at/decided_at/decided_by/reason
  -- 不删记录：谁在什么时候申请过、批没批、为什么驳回，全都留着可审计

  UNIQUE(task_id, agent)
  ├─ 一条任务只会存在一个 approved（批准时自动驳回其它 proposed）
  └─ 已批准后别的 agent 再申请 → 409

ingest_log          -- 每次收件的记录（文件名、增/改/跳过、错误）
event_log           -- 每条任务的增删改流水（谁改的、什么时候）
```

**并发安全**：WAL 模式 + `busy_timeout=15s` + 进程内写锁。
多个 agent 同时录任务不会互相覆盖——这也是不用"直接改 JS 文件"的原因。

**旧库自动迁移**：`Store._migrate()` 启动时用 `PRAGMA table_info` 比对，
缺哪列就 `ALTER TABLE ADD COLUMN` —— 老 `taskboard.db` 直接拿来用，不用手工改库。

### 5.3 字段归一化（`taskboard/model.py`）

**这是"不绑定"的核心**。不同 agent 的写法差异全在这一层抹平：

| 上游可能写 | 会被翻译成 |
|---|---|
| `name` / `task` / `标题` / `summary` | `title` |
| `zone` / `team` / `部门` / `department` | `dept` |
| `marketing` / `营销` / `市场` | `cmo` |
| `已完成` / `closed` / `resolved` / `done:true` | `status=done` |
| `doing` / `wip` / `进行中` | `status=in_progress` |
| `阻塞` / `stuck` / `paused` | `status=blocked` |
| `高` / `紧急` / `urgent:true` / `p0` | `priority=high` |
| `dept_reason` / `部门理由` | 归类理由（`dept_reason`） |
| `claim: "我的名字"` 或 `claim: {agent,note}` | 顺带提一个认领申请（见 §5.4） |
| 干脆不写部门 | 按标题关键词猜（"对账"→cfo、"部署"→cto…），猜不出进"未归类" |

**标题是唯一必填项**。其余全可省——这是让 agent 愿意上报的关键。

### 5.4 认领协议（A+B 中间态）

**设计动机**：全自动派活的代价是**拆掉人工审核关口** —— 任务会被 agent 悄悄领走，人失去筛选权。
所以这里取中间态：**agent 提建议、人拍板**。

```
agent                             看板（人）
  │  GET /api/tasks?status=todo&dept=cfo&claim=none   ← 看有哪些活
  │  POST /api/tasks/<id>/claim {agent,note}          ← 只是"申请"
  │        ↑ 任务状态**不变**，仍是 todo；queue 里也查不到
  │                                  │  左下角 🔔 亮起 / 卡片显示「🔔 N 人申请」
  │                                  │  点「批准」→ POST /api/claims/<id>/approve
  │  GET /api/queue?agent=<自己>  ← 现在能查到了（assignee 已落定）
  │  干活…  POST /api/tasks {title,status:done}        ← 按原通道回报
```

**四条不变量**（`_verify/verify_claim_flow.mjs` 逐条锁住）：

| 不变量 | 为什么 |
|---|---|
| 提申请后任务 `status` 仍是 `todo`、`assignee` 仍为空 | agent 不能自行开工 |
| 已批准后再抢 → `409` | 一条任务只能有一个执行方 |
| 批准时自动驳回同任务其它 `proposed`（reason="已被 xxx 认领"） | 避免两人同时开工 |
| 已批准的申请被同一 agent 再报一次 → `already_approved`，**不退回待批** | 曾经真出过这个 bug：等于 agent 能自己撤销批准 |

**人在看板上的三个入口**：

| 入口 | 位置 | 行为 |
|---|---|---|
| 🔔 浮层 | 左下角（在数据源指示灯上方） | 列出全部待批申请 + 申请理由，逐个批准/驳回 |
| 卡片按钮 | 房间面板里每张任务卡 | `✓` 批准 / `✕` 驳回；**只有 1 个申请人时直接处理，多个则展开浮层让人挑**（不替用户决定） |
| 已认领的 `↩` / 详情面板 | 同左 | 撤销批准 → 任务退回 `todo`、`assignee` 清空 |

**延迟**：看板默认 20 秒轮询一次，所以**别的 agent 刚提的申请最多 20 秒后出现**（`server.boardPollSec` 可调小）。

---

## 6. 目录结构速览

```
3d-taskboard-main/
├── board.config.json          ★ 唯一配置文件（改这里就够）
├── Dockerfile / docker-compose.yml / .dockerignore
├── taskboard/                 ★ 引擎（只用标准库）
│   ├── __main__.py            命令行入口：python -m taskboard …
│   ├── config.py              配置加载 + 环境变量覆盖 + 路径解析
│   ├── model.py               字段归一化 / 别名 / 自动归类
│   ├── store.py               SQLite 存取、去重、流水
│   ├── ingest.py              投递箱解析（json/jsonl/md/txt 四种）
│   ├── emit.py                导出 tasks.js / tasks.json
│   └── server.py              HTTP 服务（静态托管 + REST API）
├── agent-templates/           ★ 各 agent 接入示例
│   ├── SPEC.md                ★★ 规则唯一真源（字段/别名/两通道/判部门/认领协议）
│   ├── README.md              字段速查 + 能力档位对照
│   ├── curl.sh                shell / curl
│   ├── python-stdlib.py       零依赖 Python（也能当命令行工具用）
│   ├── node.mjs               Node 18+（自带 fetch）
│   ├── hook-session-end.sh    挂到任意 agent 的"会话结束"钩子
│   ├── windows-powershell.ps1 Windows 原生
│   ├── claim-poll.py          ★ 认领助手：ask / claim / state / queue / report / withdraw
│   ├── any-agent-prompt.md    ★ 贴进任意 agent 指令里就自动上报（含判部门 + 认领）
│   ├── 粘贴用的短口令.md      一行版 / 六行版短口令
│   ├── make-config.py         一键生成 AGENTS.md / CLAUDE.md / .cursorrules
│   ├── 一键生成配置.bat       上面那步的双击版（未实测，见 §10.13）
│   ├── 录入任务.bat           双击手动补一条（未实测，见 §10.13）
│   ├── 生成配置/              make-config.py 的实测产物（可直接复制到项目根）
│   └── dropbox/README.md      完全离线写文件的方式（含顺带提认领）
├── data/                      运行时数据（容器里挂这个卷）
│   ├── taskboard.db           任务库
│   ├── tasks.json             导出的 JSON（给别的系统读）
│   └── inbox/                 投递箱
│       └── _imported/         处理完的存档
├── tasks-data/tasks.js        ← 导出的看板数据（程序写，别手改）
└── office-3d-taskboard.html   看板本体
```

---

## 7. 如何运行 / 构建 / 部署

### A. 本机直接跑（开发/日常）

```bash
# 0) 自检：看配置、路径、依赖对不对
python -m taskboard doctor

# 1) 起服务（看板 + API + 自动收件）
python -m taskboard serve
#    → 浏览器打开 http://127.0.0.1:8787/

# 2) 灌一批演示数据看效果（可选）
python -m taskboard seed-demo
#    清掉它们：python -m taskboard clear --source demo
```

**预期结果**：终端打印服务地址与数据文件路径；浏览器打开后左下角显示
`🟢 实时同步 · N 条`，各房间挂上任务牌，右下角图例显示「未完成/总数」。

### B. 常用命令

```bash
python -m taskboard ls --dept cfo           # 列出财务的任务
python -m taskboard ls --claim=pending       # 只看有认领待批的任务
python -m taskboard add '{"title":"联调完成","dept":"cto","status":"done"}'
python -m taskboard set cfo-1a2b3c4d5e status=done    # 改状态
python -m taskboard rm  cfo-1a2b3c4d5e                # 删除
python -m taskboard ingest                            # 立刻收一遍投递箱
python -m taskboard export                            # 只重新导出 tasks.js
python -m taskboard stats                             # 统计
python -m taskboard clear --source demo               # 按来源清理
```

**认领相关（A+B 中间态）**：

```bash
python -m taskboard claim  <任务id> --agent claude-code --note "我能接"   # 提申请（状态不变）
python -m taskboard claims                                    # 看所有申请
python -m taskboard claims --status=proposed                  # 只看待批
python -m taskboard approve <认领id> --by kingsley            # 批准 → 任务转 in_progress
python -m taskboard reject  <认领id> --reason "先做别的"       # 驳回
python -m taskboard queue --agent claude-code                 # 取「已批准、可开工」的活
python -m taskboard withdraw <认领id> --agent claude-code     # 撤回申请
```

**agent 侧一站式脚本**（比上面更省事，专为"会跑命令的 agent"设计）：

```bash
python agent-templates/claim-poll.py ask    --agent claude-code --dept cfo
python agent-templates/claim-poll.py claim  <任务id> --agent claude-code --note "我能接"
python agent-templates/claim-poll.py queue  --agent claude-code
python agent-templates/claim-poll.py report <任务id> --status done --agent claude-code
```

### C. Docker 部署（推荐，适合 NAS / 24×7）

```bash
cd <本项目目录>
docker compose up -d
docker compose logs -f          # 看日志
docker compose down             # 停（数据在 ./data，不会丢）
```

**打开**：`http://<这台机器的IP>:8787/`

**数据持久化**：`./data` 挂到容器 `/app/data`；看板 HTML 与模型包也是挂载进去的，
所以**改看板不用重建镜像**，刷新浏览器即可生效。

**改端口**：改 `docker-compose.yml` 里 `ports` 左边那个数字（如 `"18787:8787"`）。
**加令牌**：改 `board.config.json → server.token`，或 compose 里的 `BOARD_TOKEN` 环境变量。

### D. 验证（改完东西跑一遍）

```bash
python _verify/test_ingest_parse.py              # 解析器单元测试 30 项（最快，秒级，改解析先跑这个）
node   _verify/verify_taskboard_api.mjs          # 端到端 25 项：接口/隐私黑名单/同步/回写/跨源/幂等
node   _verify/verify_claim_flow.mjs             # 认领闭环 25 项：agent 只能提申请 / 看板批准 / 队列 / 排他 / 撤销
node   _verify/probe404.mjs [url]                # 控制台洁净度：列出所有 4xx/5xx 与 JS 错误
node   _verify/verify_syspanel.mjs               # ⚙ 系统设置面板回归
node   _verify/shots_taskboard_api.mjs           # 截图留档
```

改看板布局时另跑 `office3d-layout-verify` 那一套（`audit_v5.py` / `verify_v5.mjs` /
`verify_walls_zfight.mjs` / `verify_v51.mjs` / `verify_doors.mjs`）。

---

## 8. 关键配置与依赖

### 8.1 唯一配置文件 `board.config.json`

| 段 | 键 | 说明 |
|---|---|---|
| `paths` | `dataDir` `dbFile` `inboxDir` `archiveDir` `emitJs` `emitJson` `boardHtml` `staticRoot` | **全部相对本文件解析** |
| `server` | `host` `port` `token` `cors` `autoIngestSec` `deny` | 服务行为；`deny` 是不对外的路径黑名单（`/` 开头按前缀匹配，其余按**子串**匹配，所以 `*.bak` 放文件名中间也能拦住；看板只放行 `.html` / `.js` / 模型文件） |
| `defaults` | `status` `priority` `project` | 上游没写时的兜底值 |
| `unassignedKey` | `"_unassigned"` | 未归类任务放这个桶（看板中控台可见） |
| `departments` | 8 个部门 id → 名称 | 要和看板 `ZONES` 的 id 一致 |
| `aliases` | 关键词 → 部门 id | 自动归类用的补充规则 |
| `limits` | `title/desc/project` 长度 | 防止超长标题撑爆卡片 |

### 8.2 环境变量（容器里首选）

| 变量 | 作用 |
|---|---|
| `BOARD_CONFIG` | 指定别的配置文件路径 |
| `BOARD_DATA_DIR` | 整体替换数据目录（db/inbox/归档/json 都跟着走） |
| `BOARD_HOST` / `BOARD_PORT` | 监听地址 / 端口 |
| `BOARD_TOKEN` | 写操作令牌 |
| `BOARD_POLL` | 看板前端轮询秒数 |

### 8.3 依赖

| 依赖 | 有无 | 说明 |
|---|---|---|
| Python 第三方包 | **无** | 纯标准库 |
| pip install | **不需要** | 构建不联网 |
| 容器内对外网络 | **不需要** | 只有浏览器端要联网加载 three.js |
| 浏览器端 CDN | three.js（esm.sh）、GSAP（cdnjs） | 首次访问需联网；之后走浏览器缓存 |

---

## 9. 数据流 / 调用链

```
① 任意 agent 干活
        │
        ├── 会发 HTTP ──► POST /api/tasks  {title, dept, status, source, …}
        │                        │
        └── 只会写文件 ─► 丢进 data/inbox/xxx.jsonl（30 秒内自动收，或 POST /api/ingest）
                                 │
                                 ▼
                    model.normalize()   ← 别名/中文/布尔旗标 全部归一到规范字段
                                 │       并记下 dept_source：上报方判的？引擎猜的？
                                 ▼
                    store.upsert()      ← 按 (dept, dedup_key) 唯一键：重复上报=更新
                                 │
                                 ├──► ingest.apply_claims()   ← 若带了 claim 字段，顺带登记认领申请
                                 ▼
                    emit.emit()         ← 写 tasks-data/tasks.js + data/tasks.json
                                 │        （含每条任务的 claim_state / claims，看板据此画徽标）
                                 ▼
        浏览器看板  ──GET /api/tasks.json──►  __applyTaskSources()  → 重绘房间任务牌
                                 │
         用户点"推进/阻塞/删除" ─► PATCH/DELETE /api/tasks/<id> ──► 回到 store（闭环）
```

**"随时查看"怎么保证**：服务端每次写入都会立刻重新导出；看板每 20 秒轮询一次，
你点按钮时还会立即回写。所以不管任务是从哪个 agent 来的，看板上最多 20 秒内可见。

**认领的调用链**（A+B 中间态，详见 §5.4）：

```
agent  ──POST /api/tasks/<id>/claim {agent,note}──►  claims 表 status=proposed
                                                       │  任务 status 仍是 todo ★
                                                       ▼
看板左下角 🔔 亮起 ──人点「批准」──POST /api/claims/<id>/approve
                                                       │
                                     tasks.assignee = agent
                                     tasks.status   = in_progress（除非已是 done）
                                     同任务其它 proposed → rejected("已被 xxx 认领")
                                                       ▼
agent  ──GET /api/queue?agent=自己──►  拿到活 → 干活 → POST /api/tasks {status:done}
```

---

## 10. 已知限制与待办

⚠️ **需要你知道的边界**

1. **未归类的任务不会落进房间**。没写部门且关键词猜不出时，任务进 `_unassigned`，
   只在 ⚙ 系统设置 →「记忆派生任务」里可见。**这是刻意的**（不猜错比猜错好），
   要归位就在录入时写 `dept`。
2. **`tasks-data/tasks.js` 现在是导出产物，不要手改**——下次任何写入都会覆盖它。
   要改数据请走 API / 命令行 / 投递箱。
3. **旧的 `inbox_tasks.js` 与 `extract-tasks.py` 仍保留**（向后兼容），
   但 API 在线时看板**不叠加**它们，避免重复计数。确认新流程稳定后可以不再用。
4. **看板前端轮询不是 WebSocket**，默认 20 秒延迟。要更实时可调 `?poll=3`，
   代价是请求变多（局域网无所谓）。
5. **`server.token` 默认是空的**。放到公网/不受信网络前**必须**设一个，
   否则任何人都能改你的任务。
6. **静态托管有黑名单**。`/data/` `/taskboard/` `/.workbuddy/` `/.git/` `/_verify/`
   `/inbox/` `/_shots/` `/agent-templates/` 等前缀，以及 `.bak` `.db` `.py` `.md`
   `.env` `.log` 等后缀/子串一律 403；只有看板真正需要的 `.html` / `.js` / 模型文件放行。
   **仍然建议**：不要把机密文件放进项目目录再对外暴露端口——黑名单是"防手滑"，
   不是安全边界。
7. ⚠️ **看板本身依赖公网 CDN**（`three.js` 从 `esm.sh`、`gsap` 从 `cdnjs`）。
   也就是说：**用 Docker 部署在纯内网 NAS 上时，浏览器必须能访问公网**，
   否则 3D 场景起不来（任务 API 不受影响）。
   要彻底离线，需把这两个库下到本地改相对路径——注意**只有走 http 托管时**才能这么改，
   双击 `file://` 打开会被浏览器拦截 ES 模块（这是当初没本地化的原因）。
8. **Docker 未在本机实测**（当前沙箱没有 docker）。`Dockerfile`/`docker-compose.yml`
   是按标准写法给的，首次部署若报错请把完整日志发回来。
9. **`taskboard.db` 是单文件依赖**，备份它就等于备份全部任务。
   但它放在 `./data` 里，WPS 云同步目录下**不要**放（云同步可能锁文件/回滚版本）——
   建议 `BOARD_DATA_DIR` 指到一个非同步目录。
10. **三个数据库表没有自动清理**，`event_log` 会随使用慢慢长大。
    目前量级（个人任务）几十年无碍；要清理跑 `sqlite3 data/taskboard.db "DELETE FROM event_log WHERE at < '2026-01-01'"`。
11. **认领是"建议制"，不是自动派活。** 看板**不会**自动把任务转给某个 agent，也不会在批准后
    主动通知那个 agent —— agent 需要自己来 `queue` 取活（或由你转达）。
    这是刻意的：按你的偏好，人工关口要留着。
12. **`claims` 表只增不删**（除非整条任务被删）。被驳回/撤回的申请留作审计痕迹，
    久了会积累；量级很小，无碍。
13. **两个 `.bat` 未在真实双击环境下实测**（开发沙箱不允许调用 `cmd.exe`）。
    写法是标准 cmd（CRLF 换行、UTF-8 + `chcp 65001`、`%~dp0` 相对定位，无硬编码路径），
    但**双击失败时请改用 `python agent-templates/make-config.py`**，功能完全一致。

---

## 回退 / 迁移

| 场景 | 做法 |
|---|---|
| 想退回旧流程 | 看板不设 `?api` 且用 `file://` 打开 → 完全走原来的静态 tasks.js |
| 换机器 | 拷贝整个项目目录 + `data/`（数据库在这），或只拷 `data/taskboard.db` |
| 数据备份 | `cp data/taskboard.db data/taskboard.db.bak`（建议定期做） |
| 清空重来 | `python -m taskboard clear --all=1`（会二次确认语义，需显式写 `--all=1`） |

**本次改动前的看板备份**：`office-3d-taskboard.html.bak_pre_syscfg_20260917`
