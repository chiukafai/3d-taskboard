# WB看板配置-001

> 文档类型：3D 办公室任务看板（office-3d-taskboard）配置梳理
> 适用范围：当前会话（2026-07-27 ~ 2026-08-02）内已完成的所有看板配置项与多轮决策
> 主文件：`office-3d-taskboard.html`
> 数据管线目录：`tasks-data/`
> 状态：✅ 本轮配置已告一段落（详见文末「五、配置收尾说明」）

---

## 一、文档概述

本文档汇总了「WorkBuddy 3D 任务看板」从布局设计、门与墙体、任务数据管线、跨部门协作、相机动画到交互方式的多轮配置过程。目标读者为 Kingsley 本人及后续接手配置的新会话，确保配置意图、关键参数与遗留事项可被准确承接。

文档结构：
- 第二节：已完成的**基础配置项**（技术栈、房间布局、门墙、数据管线、协作连线、相机交互）
- 第三节：**多轮问答关键决策与参数**（按时间线，含根因与最终取值）
- 第四节：当前遗留与待确认项
- 第五节：配置收尾与下一步接续说明（重要）

---

## 二、已完成看板基础配置项

### 2.1 技术栈与运行环境

| 配置项 | 取值 / 说明 |
|--------|------------|
| 渲染框架 | Three.js（r160+，CDN importmap 加载） |
| 控制器 | OrbitControls（左键旋转 / 滚轮缩放 / 右键平移） |
| 相机动画 | GSAP 3.12.5（CDN cdnjs），`gsap.to` + `killTweensOf` 中断式补间 |
| 拾取交互 | Raycaster（针对房间地板 `allRoomFloorMeshes`） |
| 材质 | MeshStandardMaterial + 程序化 Canvas 木纹/地毯贴图；门用 MeshBasicMaterial（不受光，避免门发黑） |
| 光源 | AmbientLight + DirectionalLight + 房间内局部 PointLight |
| 任务数据 | 外部 `tasks-data/tasks.js`（`window.TASK_DATA` / `window.TASK_META`），带 `FALLBACK_TASKS` 兜底 |
| 跨账号/机器 | 单文件 HTML + 同目录 `tasks-data/`，可复制目录整体迁移 |

### 2.2 部门房间布局（ZONES 总表，当前生效值）

> 下列参数为本会话从 `office-3d-taskboard.html` 第 285–292 行实际读取，为最新生效值。坐标单位：米。`cx/cz` 为房间中心，`w/d` 为宽/深，`doorWall` 为开门朝向，`doorPos` 为门洞中心（沿该墙的世界坐标）。

| 部门 ID | 名称 | 颜色 | cx | cz | w | d | 门墙 | 门位 | 地毯色 | 备注 |
|---------|------|------|----|----|----|----|------|------|--------|------|
| cmo | CMO 营销中心 | #b89850 | 2.75 | 9.875 | 4.5 | 19.75 | null | null | #907038 | 半开放平面（`openPlan:true`），无独立门 |
| cfo | CFO 财务中心 | #5a8070 | 9.3 | 3 | 7.8 | 7 | front | 9.3 | #406050 | 北排左 |
| coo | COO 运营中心 | #647080 | 16.2 | 3 | 6 | 7 | front | 16.2 | #4a5460 | 北排 |
| cpo | CPO 产品设计室 | #b08060 | 22.2 | 3 | 6 | 7 | front | 22.2 | #885840 | 北排 |
| cro | CRO 风控中心 | #8a5858 | 27.1 | 3 | 3.8 | 7 | front | 27.1 | #604040 | 北排右（最窄） |
| meeting | 战略会议室 | #706898 | 9.3 | 16.5 | 7.8 | 6.5 | back | 9.3 | #504870 | 南排左 |
| ceo | CEO 战略办公室 | #5a6880 | 17.1 | 16.5 | 7.8 | 6.5 | back | 17.1 | #445060 | 南排中 |
| cto | CTO 技术中心 | #485c48 | 25 | 16.5 | 8 | 6.5 | back | 25 | #304030 | 南排右 |

布局要点：
- **北排**（front 朝前）：CFO / COO / CPO / CRO，门朝 +z 方向（朝中庭）。
- **南排**（back 朝后）：会议室 / CEO / CTO，门朝 -z 方向（朝中庭）。
- **CMO** 置于最左侧竖向半开放空间，长条形（深 19.75），与北/南排通过地面连线贯通。
- 北排与南排房间**无缝相连**（行间去缝），后墙对齐。

### 2.3 门与墙体配置

| 配置项 | 取值 / 说明 |
|--------|------------|
| 门洞宽度 `gapW` | 单门 `0.8`；双门 `1.4`（当前无双开门房间） |
| 门扇宽度（**本轮修复**） | `lw = gapW / leaves`，即单门=`0.8`、双门每扇=`0.7`，**正好填满门洞**，消除门扇自由边与对侧墙体的空隙 |
| 门扇高度 | `doorH = 2` |
| 门扇厚度 | `0.06`（嵌入墙厚 0.15 内，视觉上齐平） |
| 开门角度 | 单门 `Math.PI/2.2`；双门 `Math.PI/5` |
| 门轴位置 | 固定门洞一侧边缘（前墙→左缘、后墙→右缘、左右墙同理），门扇向门洞内延伸 |
| 门材质 | `MeshBasicMaterial`（木纹贴图，不受光照，彻底避免「黑门/黑方块」问题） |
| 墙体颜色 | 深色调（各房间 `color` 对应的暗色系），背景 `#e8e3da` |
| 投影屏（会议室） | 原置于后墙（门后）造成「黑方块」，已**移至前墙** `z = hd - 0.15` |

> 修复验证：7 个带门房间（CFO/COO/CPO/CRO 前墙、会议室/CEO/CTO 后墙）门扇均已填满门洞，无间隙；CMO 无门。

### 2.4 任务数据管线

| 文件 | 作用 |
|------|------|
| `tasks-data/tasks.js` | 55 条任务，覆盖 9 个部门；每条含 `session`（会话归属）+ `source`（来源）字段 |
| `tasks-data/keymap.json` | 7 部门关键词映射，用于把对话/日志自动归类到部门 |
| `tasks-data/extract-tasks.py` | 抽取脚本，读取 `task_log.md` + `CONTEXT_INDEX` 生成 `tasks.js` |
| `tasks-data/README.md` | 迁移与接入说明 |
| `tasks-data/_update_sessions.js` | 批量注入 `session` 字段的工具 |
| `~/.workbuddy/skills/task-logger/SKILL.md` | 已加入第 8 步：记录任务后自动运行 `python3 tasks-data/extract-tasks.py` |

会话→多部门映射采用 **方法 C**（一条对话主题可归入多个部门框架，按"哪个框架分析最有价值"路由）。

### 2.5 跨部门协作连线

| 配置项 | 取值 / 说明 |
|--------|------------|
| 触发 | 任务含 `collaborators.length > 1` 时显示协作关系 |
| 联动 | `activateCollab` / `deactivateCollab` → 对应房间 `openDoor` / `closeDoor` + 生成连接线 |
| 线型 | **纯直角（Manhattan）** 地面路径，门到门，避开房间与家具，无碰撞 |
| 关系图 | 面板内以彩色圆点（collab-dot）标示协作部门 |

### 2.6 相机与交互控制

| 配置项 | 取值 / 说明 |
|--------|------------|
| 总览视角 | `animateToOverview`：相机 `(15,14,22)` → 目标 `(15,0,11)` |
| 单间聚焦 | `animateToZone`：以**门为锚点**计算机位（前墙门在 `+z`、后墙门在 `-z` 方向外推），目标为房间中心 |
| 补间参数（本轮优化） | 时长 `1.6s`；相机位移 `ease:'power3.out'`；`controls.target` `ease:'power2.inOut'`、时长 `1.6*0.9`；动画期间关闭 damping，结束恢复 |
| 可中断 | `gsap.killTweensOf(camera.position / controls.target)` 每次新动画前清空 |
| **交互方式（本轮改为双击聚焦）** | ① 单击房间 → 打开任务面板、**镜头不动**；② **双击房间 → 打开面板并 zoom in 到门前**；③ 单击空白 → 关闭面板、回到总览；④ 工具栏/图例按钮 → 单击即开面板并聚焦 |
| 提示文案 | 底部 hint：「🖱 左键旋转 ｜ 滚轮缩放 ｜ 右键平移 ｜ 单击房间看任务 · 双击房间聚焦视角」 |

---

## 三、多轮问答关键决策与参数（时间线）

> 以下按会话推进顺序摘录关键决策。重复试错类问题（如"会议室门后黑方块"历经 7 次定位）已合并为**根因结论**，不逐一罗列失败尝试。

| # | 轮次主题 | 关键决策 / 结论 | 关键参数 |
|---|---------|----------------|---------|
| 1 | 整体布局 | 放弃网格排布，改为**散点/周边式**布局，每间办公室带独立门，跨部门任务触发开门+连线 | — |
| 2 | 视觉风格 | 减少白/彩色占比，引入地毯、打印机、茶室等拟物元素，墙色加深 | 暗色系房间色 |
| 3 | 做减法 | **移除所有灯具**，加宽间距，COO 移位，中央留空，墙更深色，名牌挂外墙 | — |
| 4–8 | 任务接入 | 确立"本地任务→部门"组织方式；采用**方法 3（同步）**；明确文件以目录整体迁移即可跨账号/机器复用；其他机器需同步 `task-logger` 技能 | `tasks-data/` 整目录复制 |
| 9–10 | 面板与进度 | 新增任务面板；▶/⏸ 为状态推进/阻塞按钮；先点部门再看任务；多主题对话→按方法 C 归入多部门 | `status`: todo/in_progress/done/blocked |
| 11 | 跨部门映射 | 实现**方法 C**（会话→多部门）；面板展示关系图；内部跨部门一致性校验 | `collaborators` 数组 |
| 12 | 细节修复 | 名牌改为**双面**+可旋转+清晰字体；部门专属物品+悬浮家具；协作线由弧线改为**地面门到门** | — |
| 13 | 房间权重 | 部门按重要性定房间大小；统一单开口（去除常开门）；连线避开障碍改直角 | — |
| 14–16 | 布局重设计 | 先做 2D 平面图再审 3D；确定方案：CTO/CEO/CFO 为重点，CMO 为矩形半开放不过大 | 见 2.2 表 |
| 17 | 门联动 | 点击部门门开/关，跨部门任务**同步开关联房间门** | `openDoor/closeDoor` |
| 18–19 | 连通性 | 去茶室、行间去缝；CMO 半开放（电脑落地）；CMO 移至最左、CEO↔会议室互换、CEO-CTO 留缝 | CMO `openPlan:true` |
| 20–21 | 连线纯净 | 移除 3 棵中庭树；所有连线**纯直角 90°**；修正 CPO/CFO 门口被树/凳遮挡 | — |
| 22–24 | 墙体对齐 | 修复 CMO/CFO/会议室墙面 Z-fighting（重叠）；CMO 竖向对齐北/南排 | CMO `cz:9.875, d:19.75` |
| 25–26 | 相机动画 | 引入 GSAP 平滑过渡（ease-in-out、可中断）；扩展"门锚点 zoom"覆盖 CMO 及南排三间 | 见 2.6 |
| 27–28 | 门错位修复 | 修复门"半在墙里"：门轴由 `doorPos-(cx-hw)` 改为 `doorPos-cx`（前墙）/ `+gapW/2`（后墙）；修复重复 `const gapW` 导致的整页只显示背景 | `dpx=(doorPos-cx)∓gapW/2` |
| 29–30 | 会议室门 | 双开门需求否决 → **会议室仅保留单开门** | `gapW=0.8` 单门 |
| 31–36 | 门后黑方块 | 历经多次误判（门框/门板/光源等），**根因=投影屏置于后墙（门后）**；修复：投影屏移至前墙 `z=hd-0.15` | 屏色 `#2a2824`→前墙 |
| 37 | zoom 平滑 | 优化 Meeting/CEO/CTO 镜头过渡：初速更低、缓动更柔、时长合理、路径平滑 | `duration:1.6`, `power3.out` |
| 38 | **门缝修复（本会话）** | 检测 7 房门"门扇自由边↔对侧墙"空隙（约 0.10m）；根因=门扇宽固定 `0.72` < 门洞 `0.8`；改为 `lw=gapW/leaves` 填满门洞 | `lw = gapW/leaves` |
| 39 | **双击聚焦（本会话）** | 交互由"单击 zoom"改为"**双击部门才 zoom in**"；`openPanel` 增 `zoom` 参数，新增 `onDblClick` | 单击=开面板不移动；双击=聚焦 |

---

## 四、当前遗留与待确认项

| 项 | 状态 | 说明 |
|----|------|------|
| 双开门房间 | 未启用 | `gapW=1.4` 逻辑已就绪，但当前 8 个房间均为单门；若需双开门需补充 `doubleDoor:true` 并验证中线缝隙 |
| 名牌（nameplate）可见性 | 已修 | 双面+旋转修复在前序轮次完成，本会话未回归测试，建议新会话抽查 |
| 投影屏位置 | 已修（会议室） | 仅会议室有投影屏；其他房间若有屏需同样避免压门 |
| 任务数据时效性 | 待维护 | `tasks.js` 含 55 条（截至 2026-08-01）；新增任务需走 `task-logger`→`extract-tasks.py` 流程刷新 |
| 移动端/小屏适配 | 未覆盖 | 当前为桌面端交互（左键/右键/滚轮/双击），触屏未专门处理 |
| 视觉风格持久化到自动化 | 见全局记忆 | 黄金分析等自动化的风格变更需持久化（与本看板无直接依赖） |

---

## 五、配置收尾说明（重要）

**本轮看板配置已告一段落。** 截至 2026-08-02，以下范围均已完成并落地到 `office-3d-taskboard.html` 与 `tasks-data/`：

- 房间布局（8 部门 ZONES 参数锁定）
- 门与墙体（门扇填满门洞、无间隙；投影屏避开门）
- 任务数据管线（方法 C 映射 + 抽取脚本 + task-logger 联动）
- 跨部门协作（直角地面连线 + 门联动）
- 相机动画与交互（GSAP 平滑 + **双击聚焦**）

**下一步接续提示：**
1. 后续看板配置的**新增、调整或回归测试**，可在**新会话**中继续进行，无需重述历史——本文件（`WB看板配置-001.md`）即承接基线。
2. 新会话建议开场提供本文件路径：`/Users/chiukingsley/WorkBuddy/2026-07-27-23-18-18/WB看板配置-001.md`，并可引用项目工作记忆：`/Users/chiukingsley/WorkBuddy/2026-07-27-23-18-18/.workbuddy/memory/`（含 `2026-08-02.md` 本轮修改记录）。
3. 若涉及"门/布局/动画"之外的全新配置维度（如新增房间、接入新数据源、移动端交互），建议在新会话中先说明目标，再基于本文档第二节的现有参数做增量修改，避免覆盖已锁定配置。
4. 未完成的配置项见第四节「遗留与待确认项」，可在新会话中按优先级逐项推进。


— 文档结束 —
