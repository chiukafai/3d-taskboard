# 2026-08-10 工作日志

## 配置 board-export 技能（配合 3D 看板同步任务）

- 完成项：
  1. 安装技能：`.workbuddy/skills/board-export/SKILL.md`（副本自 `board_export_skill.md`）。
  2. 修正 frontmatter：`triggers:` → `trigger:`（单数列表，对齐 task-logger 格式，否则技能无法按触发词加载）。
  3. 修正 Windows 路径：原 `%USERPROFILE%\AI Data\...\(workbuddy)` 半角写法错误；本机实测为 `E:\AI\Workbuddy\WPS云同步（workbuddy)\...` 全角括号。
  4. 新建 `tasks-data/import_inbox.py`：解析 `inbox/任务清单_*.md` 的 ```board 块 → 生成 `tasks-data/inbox_tasks.js`（`window.TASK_INBOX`），归档源文件到 `inbox/_imported/`；stdlib-only，自动探测路径，跨树写入。
  5. 补丁 `office-3d-taskboard.html`：在 `tasks.js` 后加载 `inbox_tasks.js`；`loadTasks()` 合并 `window.TASK_INBOX`（即使 tasks.js 离线也能显示导入任务）。
  6. 创建 `inbox/` 与 `inbox/_imported/`。
- 验证：构造样例 .md → 导入 3 条成功 → node 语法校验通过 → HTML 补丁 grep 确认 → 清理测试数据，最终 `inbox_tasks.js = {}`（空，避免 404）。
- 关键发现：`workbuddy看板` 与 WPS 同步文件夹为同一目录（junction），`tasks.js` 为 WPS 云占位（Offline）文件，需"始终保留在此设备"才能被 extract-tasks.py / 浏览器读取。

## 待用户确认/后续
- `tasks.js` 离线问题：建议右键 `tasks-data/tasks.js` → 始终保留在此设备，使真实 55 条任务可加载（当前可能回退 FALLBACK_TASKS）。
- board-export 技能现可通过 `/board-export`、`/导出待办`、`/入库看板`、`/to-board` 触发。

## 移除视角自动恢复 + 看板内浏览器导入

- 改动 1（移除自动回总览）：`closePanel()` 不再调用 `animateToOverview()`，工具栏「全景」按钮改为显式调用（line 1220）。旋转后视角停在原位，不再被关面板动作拉回。
- 改动 2（浏览器内导入）：`office-3d-taskboard.html` 增加 📥 导入按钮（toolbar 281 行），用 `<input webkitdirectory>` 让用户选择 `inbox/` 文件夹 → JS 解析 ```board 块 → 合并进 `window.TASK_INBOX` + `ZONES` → 自动下载更新后的 `inbox_tasks.js`。无需终端、无需服务器。导入逻辑经 node 单元测试 6/6 通过。
- 持久化：浏览器内导入只在内存生效 + 下载 inbox_tasks.js，用户需把下载的文件放入 `tasks-data/` 并刷新页面才能持久化；与 Python 导入器行为一致。

## board-export 技能 v3.0.0 重构

按 Kingsley 反馈的 4 项需求重写：
1. **多轮动作识别**：新增 5 类信号词（完成态/执行态/决策态/未来态/责任态）+ 4 类上下文链路模式（方案→确认→执行 / 问题→调研→方案 / 探索→决定→落地 / 建议→反馈→调整）
2. **部门归纳规则**：明确 8 部门归属 + 跨部门用 `dep:` 字段；新增"归类不确定 → meeting → cto 兜底"
3. **删除时限筛选**：原 4 条筛选减为 3 条，不再要求"有时限"
4. **多目的任务处理**：默认聚合到主部门 + `dep:`；100% 完全独立才拆（提供 3 条判断模板）
- **强制产出原则**：除反思确认零信号外，所有执行必须生成 .md
- **关联 Kingsley 角色**：财务总监 + 市场营销视角，新增 7.4 节典型任务示例
- 文件已同步：`~/.workbuddy/skills/board-export/SKILL.md` + 工作区 SSOT。需新开会话加载 v3。

## 看板代码可运行性检查（16:08）

- 用户问"现在的看板代码是否能运行"。结论：**核心看板能运行并渲染办公室**。
- 校验手段：提取 `<script type="module">` 段用 `node --check --input-type=module` → SYNTAX_OK（之前导致"办公室不显示"的少 `}` 语法错误已修复）。
- 渲染管线完整：ZONES/FALLBACK_TASKS → loadTasks() IIFE(413) → scene/camera/renderer/controls(467-478) → container.appendChild(renderer.domElement)(475) → animate() 渲染循环(1540)。无运行时抛错风险点。
- ⚠️ 拖拽导入 .md 功能仍是**半成品（不报错、不生效）**：弹窗 HTML/CSS + `showImportModal`/`handleImportFolder` 函数存在，但**缺入口**——无工具栏📥按钮、无 `setupImportFeature()` 调用、无文档级 `dragover/drop` 监听、无 `buildInboxTasksJs`/`exportInboxTasksToUser` 函数。这段是死代码。
- ⚠️ 依赖 CDN：three(esm.sh)、gsap(cdnjs)。离线时 three 加载失败 → 3D 场景空白。需联网首次加载。
- 未实地用 playwright（chromium 未安装，需联网下载 ~150MB），结论基于语法+静态链路分析，置信度高但非浏览器实测。

## 补全 .md 拖拽导入功能（16:16）

- 发现：工具栏📥按钮(`btn-import`, 294行)、`#import-picker`(361)、`#drop-overlay`(362)、`handleImportFolder`/`showImportModal`/`parseImportMd` 其实**都已存在**，缺的只是"接线"——`setupImportFeature()` 没写、没调用。
- 补两处编辑：
  1. `handleImportFolder` 合并进 ZONES 后加 `if(activeZone&&ZONES[activeZone])renderPanel();`，导入后当前面板即时刷新。
  2. 新增 `setupImportFeature()`(1542行) 并调用(1571行)：绑定 📥按钮→`picker.click()`；`#import-picker` change→`handleImportFolder`；整窗 `dragenter/dragover/dragleave/drop`→显示遮罩+过滤 `.md`→`handleImportFolder`；非.md 拖入弹"未找到.md"提示。`hasFiles` 兼容 `types.includes` 与 DOMStringList 回退。
- 验证：`node --check --input-type=module` SYNTAX_OK；`parseImportMd` 单测（样例含非法部门 xxx）3 条有效任务正确解析、xxx 被跳过、优先级/状态/协作方正确。
- 残留风险：拖拽/目录选择的实际浏览器交互未实测（chromium 未装）；逻辑与标准 API 一致，置信度高。
- 临时测试文件已清理。

## 看板新增"删除任务"功能（16:27）

- 用户澄清：新增任务不需要单独做（导入功能 board-export 已覆盖，避免重复，效率优先）。范围锁定**仅删除**。
- 实现两处删除入口，共用 `deleteTask(zoneId, taskId)`（删除前 `confirm` 确认，同步刷新面板/详情/系统配置）：
  1. 部门面板任务卡：`.task-actions` 加 🗑 按钮（`data-action="delete"`），`bindPanelEvents` 新增 delete 分支。
  2. 系统配置面板：新增「🗑 部门任务管理」section（`renderSysPanel`），按部门列出任务数+任务行，每行 🗑 直接删；部门头「打开」按钮跳该部门面板（`openPanel`）。按钮用 `syspanelBody.querySelectorAll` 事件委托绑定（不依赖全局，避免 module 作用域问题）。
- CSS：`.task-action-btn.danger` + `.dept-manage/.dept-task-row/.sys-del-btn/.sys-jump` 等。
- 验证：`node --check` SYNTAX_OK。
- 行为提示：删除的是内存中 `ZONES[dept].tasks`；FALLBACK 示例任务/导入的 inbox 任务删除后刷新页面会从源数据恢复（非持久化删除）。如需持久化删除示例任务需改 `tasks.js`/`inbox_tasks.js`。

## 修复导入任务详情空白 + 各部门看板元素（16:49）

- **根因**：`parseImportMd` 把描述存成 `entry.description`，但 `openDetail` 读 `task.desc`（字段名不一致），导致点开导入任务"只有标题、无详情"。
- **修复 1（详情）**：`parseImportMd` 同时写 `entry.desc` 与 `entry.description`；`openDetail` 改为 `task.desc||task.description||'暂无详细描述。'`；部门面板卡片加 `.task-desc-line` 显示导入任务摘要（2 行截断）。
- **修复 2（3D 看板元素）**：把原"记忆墙"（`buildMemWall`，仅显示 session==='standalone' 记忆任务，fallback 数据下几乎不显示）升级为**按职能区分物理形态、显示各部门真实看板任务（含导入）**的元素：
  - `display`（cfo/cto/cpo）：壁挂显示屏 + 顶盖 + 壁挂臂。
  - `projection`（ceo/cro）：壁挂投影屏 + 顶部投影仪+镜头。
  - `whiteboard`（cmo/coo/meeting）：落地白板（支架+托盘+脚轮），置于房间内。
  - 内容来源改为 `(zone.tasks||[]).filter(t=>t.session!=='standalone')`（即看板任务；空则回退记忆任务），空部门显示「（暂无任务）」。标题改为「看板 · 部门名 (数量)」，新任务徽标改「新任务」。
  - 新增 `refreshDeptBoards()`：导入(`handleImportFolder`)与删除(`deleteTask`)后调用，3D 看板即时同步。
  - 种子 `memSeen`：示例/真实任务默认已读，仅 `id` 以 `in` 开头的导入任务脉冲提示。
- 验证：`node --check --input-type=module` SYNTAX_OK；`parseImportMd` 单测确认 `desc` 已写入且与 `description` 一致、非法部门跳过。
- 残留风险：3D 物理摆放/朝向未浏览器实测（chromium 未装）；逻辑复用既有 `drawMemCanvas`/`memWalls`/`pulseWalls`/raycast ack 体系，未破坏脉冲与点击已读。

## 优化白板位置（17:10）

- 用户反馈：① CMO 办公室与其他房间垂直（走廊型沿 Z），正面看落地白板呈侧面；② COO/会议室 白板悬在房间中央显突兀。
- 改动（`buildMemWall` 白板定位分支，仅改 x/z/rotY，mesh 几何不动）：
  - `cmo`：`x=cx+hw-0.35, z=cz, rotY=π/2` → 靠右侧墙、板面朝 +X（正对默认相机主视角，不再是侧面）。
  - `coo`：`z=cz-hd+0.6, rotY=0` → 推到后墙(-Z)倚靠，板面朝入口(+Z)。
  - `meeting`：`x=cx-2.5, z=cz+hd-0.6, rotY=π` → 靠前墙(+Z)、置于演示屏左侧，板面朝入口(-Z)；右缘 8.10 < 演示屏左缘 8.20 不重叠。
- 验证：`node --check` SYNTAX_OK；用脚本算世界 AABB，三块白板均在房间内、不穿墙（gapX≥0.10、gapZ≥0.57）、不压演示屏。临时文件已清。
- 残留风险：3D 朝向/遮挡未浏览器实测（chromium 未装）；坐标经数学验证，置信度高。

## 看板项目文件审计（23:42）

- 用户问 .bak 文件作用 + 是否有非必需文件。
- **.bak 文件本质**：`office-3d-taskboard.html` 的自动备份快照（.bak/.bak3/.bak4/.bak5/.bak6），命名带序号=典型"保留最近 N 版"编辑器/工具备份机制，用于编辑出错时回滚。
- **md5 去重**：`.bak == .bak4`（aca25686…）；`.bak5 == .bak6`（0a499272…）。5 个 bak 实际只有 4 个不同版本。当前 html(00:20)=最新内容，比 .bak 多 113 行差异。
- **非必需文件（可清理）**：5 个 .bak（仅备份）；`board_export_skill-副本20260810230030.md`（旧版 skill 副本，md5 不同、已过时）；`__pycache__/`（Python 字节码缓存，自动再生）；`.DS_Store`（macOS 元数据，根目录+inbox 各一份）。
- **待警惕的重复**：根目录 `import_inbox.py`(eca96f36…,13544B,00:20) 与 `tasks-data/import_inbox.py`(73d2feb6…,8217B,23:00) **内容不同**——后者为技能引用、较新。建议统一到 tasks-data 版，删根目录旧版，避免两版并存混淆。
- **必需文件**：office-3d-taskboard.html、tasks-data/{tasks.js,inbox_tasks.js,import_inbox.py,extract-tasks.py,keymap.json,README.md}、inbox/、.workbuddy/、board_export_skill.md。参考文档 board_capture_guide.md / WB看板配置-001.md 非运行必需但建议保留。
- 未删除任何文件，待用户确认后再清理。

## tasks-data 文件夹审计（23:48）— 澄清"3 个旧日期文件是否为废弃"

- 用户疑问：今天改过看板，但 tasks-data 下 3 个文件仍是 Jul 28 旧日期，疑似废弃。
- **结论：3 个旧文件全部仍在使用，非废弃**：
  - `extract-tasks.py`(Jul 28) = 自动管线生成器，读 `~/.workbuddy/task_log.md`/`CONTEXT_INDEX.md`/`MEMORY.md`/daily log → 产出 `tasks.js`。脚本代码未改但**今天 00:20 被运行过**（故 tasks.js 今日期）。SYNTAX_OK。
  - `keymap.json`(Jul 28) = 关键词→部门映射规则，`extract-tasks.py` 用 `load_keymap()` 加载并做 `classify_department`/`is_auto_task`。缺它会令自动管线分类失败。
  - `README.md`(Jul 28) = 迁移/使用说明文档，非运行必需但属项目文档；含一处过时绝对路径 `2026-07-27-23-18-18/`（项目已迁至 WPS云同步），无害，可选修正。
- tasks-data 6 文件中 5 个运行必需（tasks.js/inbox_tasks.js/extract-tasks.py/keymap.json/import_inbox.py），仅 README.md 为可选文档。**⚠️ 切勿因"旧日期"删除 extract-tasks.py 或 keymap.json，否则自动管线断链。**

## 创建项目说明文档（23:53）

- 用户要求：为项目建一份说明文档，列明各文件主次关系与作用。
- 新建 `项目说明.md`（根目录）：①简介 ②六层文件主次层级图 ③文件清单总表（层级/作用/必需性/备注）④双数据管线图（自动 extract 管线 + 手动 import 管线）⑤日常操作速查 ⑥踩坑预警（tasks.js WPS 占位需"始终保留"、勿删 extract/keymap、双 import_inbox.py 并存、CDN 联网、bak 去重）⑦非技术同学速记类比。
- 归类：核心(①)、数据管线(②)、输入源(③)、配置技能(④)、参考文档(⑤)、可清理冗余(⑥)。明确根目录旧 import_inbox.py 与 board_export_skill-副本 为可清理项。
