# 2026-08-11 工作日志

## WPS 云同步冲突导致 Windows 早上修改被覆盖（00:01）

- 事件：用户在 Windows 今早修改看板，仅在本机(Mac)打开页面查看效果；WPS 云文件夹日期变"刚刚"，Windows 修改被 Mac 旧版（office-3d-taskboard.html = Aug 10 00:20）覆盖，云端历史版本找不到。
- 核实（Mac 本机）：`office-3d-taskboard.html` / `tasks-data/tasks.js` = Aug 10 00:20（含本会话早前改动，但**不含 Windows 早修改**）；`inbox_tasks.js`/`import_inbox.py`/`board_export_skill.md` = Aug 10 23:00（本会话改）。`.bak` 均更早，**无任何 Windows 修改副本**。
- 根因：WPS 云同步 last-write-wins；Mac 本地为旧版(00:20)，"仅打开页面"触发 WPS 回传本地旧版、覆盖云端 Windows 修改。
- 恢复关键：Windows 本机本地副本大概率仍存今早修改——**前提是其 WPS 尚未把云端坏版本拉下来**。须立即去 Windows 把文件夹复制出非同步区（桌面/U盘/非同步盘）。WPS 对 .html/.js 通常**不保留历史版本**，云端找回希望小。
- 预防：跨机编辑前确认 WPS 状态=已同步；只看效果就开副本、不开同步文件；重要修改后 zip 到非同步区；建议项目用 git 做版本控制。
- 本次未做任何文件修改（避免触发进一步同步）。
