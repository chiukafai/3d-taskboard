# 3D 办公室布局修正说明书

> 修正日期：2026-09-15
> 修正文件：`office-3d-taskboard.html`
> 备份文件：`office-3d-taskboard.html.bak_pre_doorface_20260915`

---

## 一、项目概述

本项目是一个 3D 办公室任务看板（基于 three.js），将 7 大职能（CEO / CFO / CTO / CPO / CMO / COO / CRO + 会议室）的任务可视化呈现为虚拟办公室。

本次修正针对用户从 3D 渲染图中发现的 3 个布局逻辑问题进行整改。

---

## 二、修正内容

### 问题 1：部门领导座位背对房门（⚠️ 高优先级）

**修正前**：所有 7 间办公室的领导座位均背对房门、面朝办公桌（原设计注释中的"铁律"）。
**修正后**：领导座位改为面朝房门、背靠远墙（经典"权力位"布局）。

#### 修正明细表

| 房间 | 门墙方向 | 修正前（桌/椅位置, 旋转） | 修正后（桌/椅位置, 旋转） | 领导面朝 |
|------|----------|--------------------------|--------------------------|----------|
| CPO 产品设计室 | 右墙(+X) | 桌[-1.70,0]r-π/2 / 椅[-1.15,0]r-π/2 | 桌[-1.15,0]rπ/2 / 椅[-1.70,0]rπ/2 | +X(门) ✓ |
| CFO 财务中心 | 右墙(+X) | 桌[-1.90,0.30]r-π/2 / 椅[-1.35,0.30]r-π/2 | 桌[-1.35,0.30]rπ/2 / 椅[-1.90,0.30]rπ/2 | +X(门) ✓ |
| CRO 风控中心 | 前墙(+Z) | 桌[0,-1.50]rπ / 椅[0,-0.95]rπ | 桌[0,-0.95]r0 / 椅[0,-1.50]r0 | +Z(门) ✓ |
| CMO 营销中心(工位1) | 前墙(+Z) | 桌[-1.70,-1.90]rπ / 椅[-1.70,-1.35]rπ | 桌[-1.70,-1.35]r0 / 椅[-1.70,-1.90]r0 | +Z(门) ✓ |
| CMO 营销中心(工位2) | 前墙(+Z) | 桌[1.30,-1.90]rπ / 椅[1.30,-1.35]rπ | 桌[1.30,-1.35]r0 / 椅[1.30,-1.90]r0 | +Z(门) ✓ |
| CTO 技术中心 | 前墙(+Z) | 桌[0.60,-1.30]rπ / 椅[0.60,-0.75]rπ | 桌[0.60,-0.75]r0 / 椅[0.60,-1.30]r0 | +Z(门) ✓ |
| COO 运营中心 | 后墙(-Z) | 桌[0.30,-1.20]r0 / 椅[0.30,-1.80]r0 | 桌[0.30,-1.80]rπ / 椅[0.30,-1.20]rπ | -Z(门) ✓ |
| CEO 战略办公室 | 后墙(-Z) | 桌[-0.90,-1.15]r0 / 椅[-0.90,-1.75]r0 | 桌[-0.90,-1.75]rπ / 椅[-0.90,-1.15]rπ | -Z(门) ✓ |

#### 同步更新的代码位置

1. **SHOW 数组**（`CPO_SHOW` ~ `CEO_SHOW`）：8 组 `exec_desk` + `exec_chair` 的 `p`（位置）和 `r`（旋转）
2. **`deskGrp()` 调用**：8 处桌面组（含显示器/键盘/鼠标等）的位置和旋转
3. **CFO 程序兜底家具**（`if(!hasModels)` 内）：`createExecDesk`、`createExecChair`、`createLaptop`、`createSofa`、`createFilingCabinet` 坐标
4. **CFO 桌面物品**（`deskProps` 组）：键盘、鼠标、电话、笔筒、马克杯、文件堆、托盘共 7 件——位置按桌面中心镜像重算
5. **CFO 主机箱**（`createTowerPC`）：位置从 [-1.95, 1.20] 移至 [-1.40, 1.20]
6. **CMO 程序兜底**：两组工位 z 坐标从 -1.90 更新为 -1.35

### 问题 2：门口附近摆放物品阻挡通行（⚠️ 高优先级）

**修正前**：文件柜、绿植等物品紧贴门墙摆放，视觉上阻碍入口。
**修正后**：门口留白，物品移至远离门墙的位置。

#### 修正明细表

| 房间 | 物品 | 修正前位置 | 修正后位置 | 移动距离 |
|------|------|-----------|-----------|----------|
| CPO | 文件柜 | [2.90, -1.70] | [1.50, -2.20] | 远离右墙 1.40m |
| CPO | 绿植 | [2.80, -0.20] | [1.50, -0.20] | 远离右墙 1.30m |
| CFO | 文件柜 | [2.55, -2.20] | [1.50, -2.30] | 远离右墙 1.05m |
| CFO | 打印机 | [2.65, -1.55] | [2.00, -1.55] | 远离右墙 0.65m |
| COO | 文件柜×2 | z=-3.05 | z=-2.00 | 远离后墙 1.05m |
| CEO | 文件柜 | [2.95, -2.60] | [2.95, -1.50] | 远离后墙 1.10m |
| CEO | 绿植 | [-2.85, -2.85] | [-2.85, -1.50] | 远离后墙 1.35m |

### 问题 3：沙发与茶几朝向不匹配（⚠️ 高优先级）

**修正前**：CFO 和 CEO 的沙发朝向偏离茶几，无法形成合理的会客对话格局。
**修正后**：沙发正面朝向茶几，恢复"对坐会谈"的社交逻辑。

#### 修正明细

| 房间 | 沙发修正前 | 沙发修正后 | 茶几位置 | 朝向关系 |
|------|-----------|-----------|---------|----------|
| CFO | [-1.10,-1.75] r=0（面朝+Z南） | [-1.10,-1.75] r=π/2（面朝+X东） | [-0.30,-1.75] | 沙发面朝+X→正对茶几 ✓ |
| CEO | [1.45,1.55] r=π（面朝-Z北） | [1.45,1.55] r=0（面朝+Z南） | [1.45,0.55] | 沙发面朝+Z→正对茶几 ✓ |

> 注：会议室（两沙发对坐）、CMO（沙发面朝+X茶几）、COO（沙发面朝+X茶几）的朝向原本正确，未做修改。

---

## 三、技术栈

- **三维引擎**：three.js r0.157（从 CDN 加载）
- **动画库**：gsap
- **模型格式**：GLB（base64 内嵌于 `cfo-models.js` / `all-models.js`）
- **模型加载**：GLTFLoader + DRACOLoader
- **数据格式**：JavaScript 全局变量（`window.TASK_DATA`、`window.OFFICE_MODELS`）

---

## 四、修改涉及的代码区域

| 代码区域 | 行号范围（约） | 修改内容 |
|---------|-------------|---------|
| 布局注释（铁律） | 1300-1307 | "背对门"→"面朝门、背靠远墙" |
| `CPO_SHOW` 数组 | 1308-1316 | 桌椅交换+旋转、文件柜/绿植移位 |
| `CFO_SHOW` 数组 | 1327-1338 | 桌椅交换+旋转、沙发朝向、文件柜移位 |
| `CRO_SHOW` 数组 | 1339-1347 | 桌椅交换+旋转 |
| `CMO_SHOW` 数组 | 1348-1364 | 两组桌椅交换+旋转 |
| `CTO_SHOW` 数组 | 1365-1375 | 桌椅交换+旋转 |
| `COO_SHOW` 数组 | 1376-1388 | 桌椅交换+旋转、文件柜移位 |
| `CEO_SHOW` 数组 | 1389-1399 | 桌椅交换+旋转、沙发朝向、文件柜/绿植移位 |
| CFO 程序兜底家具 | 1632-1663 | 桌椅/沙发/文件柜/打印机/主机箱/桌面物品全部跟随新坐标 |
| CMO 程序兜底 | 1665-1678 | 工位 z 坐标更新 |
| 各房间 `deskGrp()` | 1680-1715 | 8 处桌面组位置+旋转同步更新 |

---

## 五、运行方式

1. 双击 `office-3d-taskboard.html` 打开（首次需联网加载 CDN）
2. 模型数据从 `cfo-models.js`（或 `all-models.js`）内嵌加载
3. 如模型加载失败，自动回退到程序化兜底家具（本次已同步修正坐标）

---

## 六、环境配置

- 浏览器：Chrome / Edge / Firefox（需支持 WebGL）
- 首次打开需联网（CDN 加载 three.js）
- 后续可离线运行（CDN 缓存）

---

## 七、已知限制

1. **墙面显示屏朝向**：领导改面朝门后，壁挂显示屏在远墙（领导身后），需转身查看。如需"既面门又看屏"，需将显示屏移至门侧墙（本次未改，留待用户决定）
2. **访客椅**：部分房间访客椅（office_chair）朝向未调整，可能不完全正对新桌位
3. **程序兜底家具**（非 CFO 房间）：CEO/CRO/CPO 等的程序兜底桌椅用 `hd` 相对坐标，未逐间翻转旋转（仅在模型加载失败时可见，概率极低）

---

## 八、回退方案

如需回退至修正前版本：
```
office-3d-taskboard.html.bak_pre_doorface_20260915
```
复制此备份覆盖当前文件即可。

---

## 九、交接说明（供其他 AI agent 接手）

1. **朝向约定**（实测得到，勿改）：
   - `exec_chair` / `sofa`：r=0 面朝 +Z（靠背在 −Z）
   - `office_chair`：r=0 面朝 −Z（靠背在 +Z，与 exec_chair 相反）
2. **新铁律**（2026-09-15 起）：椅贴远墙、人面朝门、桌在椅与门之间
3. **门向↔人朝向映射**：front(+Z)→r=0；back(−Z)→r=π；right(+X)→r=π/2
4. 修改 SHOW 数组后，务必同步修改对应的 `deskGrp()` 调用
5. CFO 房间的 `deskProps` 组使用房间绝对坐标（非桌面局部坐标），桌面移动后需按镜像公式重算
6. 镜像公式：新x = -旧x - 3.25，新z = 0.60 - 旧z，旋转 += π
