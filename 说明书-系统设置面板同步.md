# 说明书 · 看板「⚙ 系统设置（中控台）」配置同步

> 适用产物：`office-3d-taskboard.html` 的 `#syspanel` 面板 + `_verify/sync_sysconfig.py`
> 建立日期：2026-09-17 · 维护人：Kingsley 的 AI 搭档

---

## 1. 概述与用途

看板右上角的 **⚙ 系统** 面板里有一组「人格配置文件」卡片（SOUL / IDENTITY / USER / MEMORY），
点开能看到这四份文件的**全文**。

问题在于：**浏览器打不开你自己电脑上的文件**。所以看板只能把这四份文件的内容**预先抄一份**写进 HTML ——
就像把合同正本复印一份贴到公告栏上。**正本改了，公告栏上的复印件不会自己变。**

这套工具解决的就是「复印件 vs 正本」的对齐问题：

| 工具 | 干什么 |
|---|---|
| `_verify/sync_sysconfig.py` | 重新复印：把 `~/.workbuddy/` 里四份正本的最新内容重抄进看板，并写准日期 |
| `_verify/cmp_sysconfig.py` | 比对：逐行列出复印件与正本的差异 |
| `_verify/verify_syspanel.mjs` | 验电器：检查面板能不能打开、卡片在不在、弹窗点得开吗 |

---

## 2. 技术栈总览

| 层 | 技术 | 用途 |
|---|---|---|
| 前端 | 原生 JS（ES Module）+ three.js | 3D 看板主程序，`<script type="module">` |
| 面板 UI | 原生 DOM + 内联 CSS | `#syspanel` 中控台侧边栏 |
| 配置存储 | HTML 内嵌 `<script type="text/plain" id="cfg-XXX.md">` | 四份配置的"复印件" |
| 同步工具 | Python 3.13（标准库 `re` / `difflib`） | 重抄 + 比对 |
| 校验工具 | Node 22 + Playwright（headless Chrome） | 面板生效性回归与截图 |

---

## 3. 编程语言与版本

- **Python 3.13.12**（WorkBuddy 托管版，仅用标准库，无第三方依赖）
- **Node 22.22.2** + Playwright（复用项目 `_verify/` 下既有环境）
- **JavaScript ES2020**（浏览器内，无构建步骤）

---

## 4. 数据结构

### 4.1 面板内的配置索引（HTML 第 ~2157 行）

```js
const SYS_CONFIG_FILES = [
  { name:'SOUL.md', role:'灵魂/行为准则', mtime:'2026-08-04',
    path:'~/.workbuddy/SOUL.md', desc:'…' },
  // IDENTITY.md / USER.md / MEMORY.md 同构
];
const SYS_SYNC_DATE = SYS_CONFIG_FILES.map(f=>f.mtime).slice().sort().pop();
//                   ↑ 同步基准日 = 四份里最新的那份日期，同步脚本自动写入，无需手工维护
```

- `mtime` —— **卡片上显示的「更新于」日期**，由脚本按真实文件修改时间填写。
- `desc` —— 卡片上的一段话简介，**需人工核对是否与文件现行内容相符**（脚本不校验这段）。

### 4.2 内嵌正文（HTML 第 2609–3081 行）

```html
<script type="text/plain" id="cfg-SOUL.md">
（SOUL.md 的完整原文）
</script>
```

用 `type="text/plain"` 是为了：① 浏览器不解析、不执行；② JS 可直接 `textContent` 取出；
③ 无需转义引号换行，贴进去就是原文，人工 diff 也方便。

---

## 5. 数据流 / 调用链

```
平时（看板运行）
  点 ⚙ 系统  →  openSysPanel()  →  renderSysPanel()
                                  ├─ 用 SYS_CONFIG_FILES 渲染 4 张卡片
                                  └─ 用 ZONES / TASK_DATA / TASK_INBOX 渲染另三个分区
  点某张卡片  →  内联 onclick="openCfgModal('SOUL.md')"
                                  →  document.getElementById('cfg-SOUL.md').textContent
                                  →  renderMarkdown()  →  弹窗显示全文

更新配置后（离线工具）
  ~/.workbuddy/*.md 有改动
        ↓  人工执行
  python _verify/sync_sysconfig.py
        ├─ ① 替换 HTML 里 4 个 script 块的正文
        └─ ② 改写 SYS_CONFIG_FILES[].mtime 为真实 mtime
        ↓
  node _verify/verify_syspanel.mjs   （确认面板仍生效）
```

---

## 6. 如何运行

```bash
# 0) 前置：起本地预览服务（校验脚本依赖 8934 端口）
python -m http.server 8934 --bind 127.0.0.1

# 1) 先看差多少（只读，不改文件）
python _verify/sync_sysconfig.py --check

# 2) 要逐行差异明细时
python _verify/cmp_sysconfig.py

# 3) 同步（会写回 HTML，执行前请确认已有备份）
python _verify/sync_sysconfig.py

# 4) 校验面板是否真的生效
node _verify/verify_syspanel.mjs

# 5) 截图留档（面板 + 弹窗）
node _verify/shots_syspanel.mjs

# 附带：查加载失败资源与任务数据状态
node _verify/diag_404.mjs

# 对另一份看板（如 E 盘主项目）执行同样操作：把 HTML 路径当参数传
python _verify/sync_sysconfig.py "E:/AI/Workbuddy/workbuddy看板/office-3d-taskboard.html"
```

**预期结果**：第 1 步输出 `本就一致`、退出码 0；第 4 步四项 `✅`、`新增报错: 无`。

---

## 7. 关键配置

| 项 | 值 | 说明 |
|---|---|---|
| 配置源目录 | `~/.workbuddy/` | 可用 `HOME_WB` 常量改 |
| 同步文件清单 | `FILES = [SOUL, IDENTITY, USER, MEMORY].md` | 想加文件就改这个列表 |
| 默认目标 | `../office-3d-taskboard.html` | 可命令行传路径覆盖 |
| 预览端口 | 8934 | 与项目其他校验脚本一致 |

**无密钥、无外网依赖**，全部本地文件操作。

---

## 8. 已知限制与待办

1. **同步是手动的**。真实配置文件改动后，看板不会自动更新，必须跑一次脚本。
   面板顶部已加提示条说明这一点并给出命令，避免"以为它自动跟着变"。
2. **卡片上的 `desc` 一句话简介不自动生成**，文件内容大改后需人工核对措辞。
3. **`tasks-data/tasks.js` 在 E 盘是 WPS 云占位文件（读不出来）** →
   「📄 记忆派生任务」分区恒为 0 条。需在 WPS 里把该文件下载到本地后重跑任务管线。
4. **工作区副本与 E 盘主项目已分叉**（工作区 09-17 / E 盘 09-10）。同步脚本两边都能跑，
   但执行前需先定"以哪一份为准"，否则 WPS 云同步的 last-write-wins 可能吃掉改动。
5. 面板的「✅ 本会话已完成」是 `SYS_COMPLETED` 硬编码数组，**不会自动更新**，属展示性内容。

---

## 9. 回退方案

```bash
cp office-3d-taskboard.html.bak_pre_syscfg_20260917 office-3d-taskboard.html
```

备份文件与改动同日生成，覆盖即回到本次修改前的状态。
